# Event signup integration evidence for #153

This record maps all 67 User Stories and 28 behavioral checks from #145 to the completed slices and integration evidence. It records the limits of the test platforms. It does not change the parent specification.

## Integration run on 2026-09-06

The isolated database is `bracketiq_e2e_153_codex` on local PostgreSQL port 5433. All 234 migrations applied. Prisma reported no pending migration. The final optimized Next build passed, including TypeScript and 128 static pages. The local production server uses port 3153. Outbound providers are disabled. Imported document evidence uses the existing local storage provider.

The desktop and narrow browser created a Team and a no-email Player. The saved draft reached review without registration. Android resumed that draft and confirmed registration. The site then showed one registered Team and no remaining draft. A returning manager completed review and confirmation in the narrow browser. The reverse journey also passed: Android saved Harbor Cup at review with Summit United selected. The browser restored that selection and confirmed one registration.

A separate manager Account completed Team creation, no-email Player addition, review, and confirmation entirely in Android. The site confirmed the same registration. A browser opened that Player's signed claim link. Sign-in preserved the link. The form required birthdate and explicit confirmation. Successful claim continued to a separate Team acceptance step. Android then accepted the Team invitation. PostgreSQL confirmed ACCEPTED, the existing Account as actor, and one ACTIVE roster entry.

The final metadata APK passed. Android displayed Indoor Volleyball, Teams of 8, and 1/16 registered Teams. This verifies that a partial participant refresh preserves the complete Event. The later native setup-roster check found incorrect Invite Sent labels. The screen now uses active roster IDs and persisted invitation labels. It also shows Managed profile independently. The final APK removed pending badges from accepted Players and showed Awaiting guardian for the pending child. Membership normalization now preserves the server invitation ID and label.

Database results: draft 14/14; retention 10/10; combined privacy, lifecycle, and cutover 63/63. The earlier combined conversion and profile-claim run passed 14/14: nine cutover checks and five claim checks. The latest affected run passed ten cutover checks and five claim checks. These cover stale unbound email attempts, atomic rollback, independent Event registration preservation, concurrent claim, primary identity and block preservation, first and returning guardians, and the age-18 authority boundary.

All four live mobile-to-site suites passed: Event signup, invitation lifecycle, invitation retention, and operational roster. The operational test completed Player and guardian signing, imported evidence, profile merge, and Room refresh. The full site run completed with 5,970 passed tests, two environment-caused email failures, and 128 skipped tests. Both email tests passed with their normal test environment. Stale internal fixtures and roster assertions were repaired and passed. The full mobile test run passed 1,917 tests and skipped 16 live-environment tests. The four feature-specific live suites passed separately. Android lint and APK packaging passed. Native iOS was not tested.

Temporary screenshots are under `%LOCALAPPDATA%/Temp/bracketiq-issue153`. Test and build logs are under `.scratch/issue153-*`. These temporary files are not release artifacts. The release gates are in `event-signup-cutover-checklist.md`.

## Evidence sources

- S: `apps/site/src/server/__tests__/eventRegistrationDraft.database.integration.test.ts`; mobile `EventSignupMobileApiIntegrationTest`; site and mobile registration UI tests.
- L: `apps/site/src/server/teams/__tests__/invitationLifecycle.database.integration.test.ts`; mobile `InvitationLifecycleMobileApiIntegrationTest` and `InvitationRoomPersistenceTest`; invitation route and roster tests.
- I: `apps/site/src/server/__tests__/profileClaimJourney.database.integration.test.ts`; `apps/site/src/server/__tests__/eventSignupCutover.database.integration.test.ts`; `apps/site/src/server/__tests__/managedPlayers.test.ts`; `apps/site/src/app/api/team-invites/[id]/claim/__tests__/route.test.ts`; member invitation route tests. Mocked tests require stronger persisted claim and conversion evidence for #153.
- G: `apps/site/src/server/__tests__/profileClaimJourney.database.integration.test.ts`; `apps/site/src/app/api/family/__tests__/guardianClaimsRoute.test.ts`, `childrenRoute.test.ts`, `apps/site/src/app/api/auth/__tests__/guardianVerificationContinuation.test.ts`; site and mobile guardian UI tests.
- R: `apps/site/src/server/events/rosterDocumentReadiness.ts`, operational roster route tests; mobile `OperationalRosterMobileApiIntegrationTest`; `apps/site/scripts/test-operational-roster-fixtures.ts`.
- H: `apps/site/src/server/__tests__/invitationRetention.database.integration.test.ts`; mobile `InvitationRetentionMobileApiIntegrationTest`.

## User Stories

| ID | Requirement | Slice | Existing evidence | Integrated evidence and platform limits |
|---|---|---|---|---|
| 1 | As a new Team manager, I want Event signup to show the next required action, so that I can complete registration without knowing the Team-management structure. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 2 | As a new Team manager, I want to create a Team inside the Event journey, so that I do not lose the Event I intended to join. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 3 | As a new Team manager, I want to enter only required Team details, so that optional setup does not delay registration. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 4 | As a Team manager, I want to add Players after Team creation or continue without them, so that I can register before completing roster administration. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 5 | As a Team manager, I want Continue to event to select the new Team, so that I do not need to find and select it again. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 6 | As a Team manager, I want to review the Event registration before I confirm it, so that creating a Team does not register or charge me automatically. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 7 | As a Team manager, I want staff setup and free-agent discovery to remain available outside the required journey, so that I can use those features when needed. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 8 | As a returning Team manager, I want my last eligible Team from a completed registration in the same sport selected, so that repeated signup is fast. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 9 | As a returning Team manager, I want Change team and Add players on Event review, so that a remembered Team does not prevent a different choice. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 10 | As a Team manager without an eligible remembered Team, I want the sole eligible Team selected or a compact choice when several qualify, so that the fallback is clear. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 11 | As a Team manager, I want my unfinished Event's Team selection kept separate from my completed-registration preference, so that an abandoned signup does not change later defaults. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 12 | As a Team manager, I want to resume an unfinished registration on site or mobile, so that switching devices does not repeat completed steps. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 13 | As a Team manager, I want saved Teams, Players, and invitations to remain when I leave signup, so that completed work is not discarded. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 14 | As a Team manager, I want resume and retry to avoid duplicate Teams and invitations, so that a connection failure does not create duplicate work. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 15 | As a Team manager, I want Event availability and Team eligibility checked again when I resume, so that stale progress cannot complete an invalid registration. | #151 | S | Manager UI journeys passed at desktop and narrow widths and on Android. Both resume directions and explicit confirmation passed. Draft database checks passed 14/14. |
| 16 | As a Team manager, I want an added Player to appear on the roster immediately, so that the roster reflects the people I intend to bring. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 17 | As a Team manager, I want an unaccepted Player to reserve a Team roster slot, so that pending invitations cannot exceed roster capacity. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 18 | As a Team manager, I want one list for accepted and unaccepted Players, so that I do not have to compare separate rosters. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 19 | As a Team manager, I want Managed profile shown separately from invitation status, so that I can distinguish Account ownership from acceptance. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 20 | As a Team manager, I want Awaiting player, Awaiting guardian, and Invitation expired states, so that I know which action is still required. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 21 | As a Team manager, I want Share invite link for a Player without email, so that the Player can claim their existing profile later. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 22 | As an invited Player, I want Team permissions withheld until acceptance, so that a manager cannot grant participation access on my behalf. | #146, #147, #149 | L | Native manager roster creation and browser claim passed. Lifecycle capacity and denied-access tests passed. Native recipient acceptance passed. The final Android roster labels passed. Accepted Players have no pending badge. Managed profile is separate from Awaiting guardian. |
| 23 | As an authorized Event Host, I want the full Event roster, including unaccepted and Managed Players, so that I can prepare for the actual participants. | #152 | R | Privacy and live operational roster HTTP/Room checks passed. Earlier #152 host and official screenshots remain available. |
| 24 | As a match official, I want the complete roster and readiness status for my relevant match, so that I can perform my assigned duties. | #152 | R | Privacy and live operational roster HTTP/Room checks passed. Earlier #152 host and official screenshots remain available. |
| 25 | As an invited Player, I want my unaccepted identity hidden from regular Team members and public roster viewers, so that an invitation does not expose me to those audiences. | #146, #149, #151 | L, S | Privacy, draft, and cutover database checks passed. The installed APK shows accepted Players without pending badges. |
| 26 | As a Team manager, I want a Player added during signup assigned to the Team and the current Event only, so that another upcoming Event's roster does not change silently. | #146, #149, #151 | L, S | Privacy, draft, and cutover database checks passed. The installed APK shows accepted Players without pending badges. |
| 27 | As a Team manager, I want accepted Players to lose pending invitation badges, so that completed acceptance is clear. | #146, #149, #151 | L, S | Privacy, draft, and cutover database checks passed. The installed APK shows accepted Players without pending badges. |
| 28 | As a Team manager, I want to add a real Player without creating an Account, so that roster work does not depend on the Player signing up first. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 29 | As a Player without an Account, I want to claim my existing User Profile, so that my roster associations remain attached to the same person. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 30 | As a Player, I want authentication and explicit claim confirmation, so that opening a link alone cannot attach a profile to an Account. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 31 | As a Player with an attached personal email, I want that address verified during claim, so that a forwarded URL cannot bypass proof of email control. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 32 | As a Player without an attached personal email, I want a valid claim URL to work after authentication and confirmation, so that email is not required for every Managed Player. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 33 | As a Player with an existing claimed profile, I want a verified merge into that primary profile, so that I do not end up with two identities. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 34 | As a Player with an existing claimed profile, I want my personal details preserved during merge, so that manager-entered values do not overwrite my own details. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 35 | As a Player, I want profiles kept separate unless claim proof and explicit confirmation support a merge, so that people with matching names are not combined. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 36 | As a Team manager, I want to correct an unverified contact on an unclaimed profile, so that an address error does not require a duplicate Player. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 37 | As a Player, I want old claim links invalidated after an authorized contact correction, so that an earlier recipient cannot use stale access. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 38 | As a Player with verified contact details or a claimed profile, I want changes restricted to myself, an authorized guardian, or support, so that a Team manager cannot take control of my identity. | #147 | I | The latest cutover and claim checks passed 15/15: ten cutover checks and five claim checks. Browser email-bound and no-email claims passed. Live merge and Room refresh passed. Android accepted the resulting invitation. Direct native signed-link entry was not tested; see Platform limits. |
| 39 | As a Team manager, I want an Is a minor checkbox in the new Player form, so that I can direct acceptance to a guardian. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 40 | As a Team manager adding a minor, I want birthdate and guardian email required, so that the child profile and acceptance route are established together. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 41 | As a guardian of several children, I want one Guardian Contact usable for separate child profiles, so that siblings do not need unique parent email addresses. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 42 | As a guardian, I want the invitation to identify the named child, so that accepting for one child cannot claim the child as my own identity or affect a sibling. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 43 | As a first-time guardian, I want one guided sign-in, email verification, child review, declaration, and acceptance journey, so that I can finish without navigating separate setup screens. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 44 | As a returning guardian, I want my existing active Guardian Relationship reused, so that I do not repeat completed setup. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 45 | As a Player whose birthdate is missing, I want birthdate collected before acceptance authority is selected, so that an unchecked manager checkbox is not treated as proof of adulthood. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 46 | As a minor with an Account, I want guardian-required Team acceptance to remain in effect, so that Account creation does not bypass the age rule. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 47 | As a Player turning 18, I want access granted solely through guardianship to end, so that I control my adult profile while relationship and document history remain. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 48 | As an adult without an Account, I want to claim my existing child profile, so that becoming an adult does not create another Player identity. | #148 | G | First and returning guardian database checks passed. Narrow first-guardian and desktop returning-guardian UI passed. The controlled age-18 database check passed. Android reused the browser-established relationship to accept a new invitation for the named child. Direct native first-guardian link entry was not tested. |
| 49 | As an authorized Event Host, I want missing signatures reported for unaccepted and Managed Players, so that an absent Account cannot hide an incomplete requirement. | #152 | R | Live operational roster test passed: Player signing, guardian signing, approved import, merge, and Room refresh. |
| 50 | As a Player or authorized guardian, I want to complete the required signing flow, so that the correct person's evidence satisfies the requirement. | #152 | R | Live operational roster test passed: Player signing, guardian signing, approved import, merge, and Room refresh. |
| 51 | As an authorized Organization staff member, I want supported externally signed evidence attached through the approved import and attestation model, so that valid evidence can satisfy a Managed Player's requirement. | #152 | R | Live operational roster test passed: Player signing, guardian signing, approved import, merge, and Room refresh. |
| 52 | As an Event Host, I want document completion to preserve required Signer roles, Version, and scope, so that a Team manager cannot mark a requirement complete without evidence. | #152 | R | Live operational roster test passed: Player signing, guardian signing, approved import, merge, and Room refresh. |
| 53 | As a Team manager, I want missing-document visibility without new registration or participation blocks, so that this feature does not silently change Event policy. | #152 | R | Live operational roster test passed: Player signing, guardian signing, approved import, merge, and Room refresh. |
| 54 | As an invited Player, I want plain Decline to remove the current roster entry, so that I am not listed as a current Player after rejecting the invitation. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 55 | As a Team manager, I want a new invitation allowed immediately after ordinary decline, so that an accidental decline can be corrected even when join requests are disabled. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 56 | As a recipient, I want each new attempt to keep its own outcome, so that repeated invitations do not overwrite my earlier declines. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 57 | As a Team manager, I want a reminder to remain part of the current pending attempt, so that delivery history does not create duplicate roster entries or invitations. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 58 | As a Team manager, I want invitation expiry to keep the Player on the roster with an expired label, so that lack of a response is not treated as rejection. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 59 | As a recipient, I want Decline and block to let me select the sender or the Team, so that I can choose the scope of unwanted contact. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 60 | As a recipient, I want a Team Block to stop additions, invitations, and reminders from every manager of that Team, so that changing the sender cannot bypass the restriction. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 61 | As an Account holder, I want User Blocks to target active Accounts only, so that an inactive, unclaimed User Profile is not treated as a social Account. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 62 | As a recipient blocking a sender, I want Leave all chats with this user unchecked, so that blocking an invitation does not leave shared chats without my explicit choice. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 63 | As a guardian, I want Block sender applied to my Account and Block Team applied to the named child and Team, so that the two choices have clear, different scopes. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 64 | As a Player or authorized guardian, I want to remove a Team Block, so that I can permit future invitations without erasing previous outcomes. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 65 | As a Player merging a profile, I want my existing Account's blocks preserved, so that identity cleanup cannot remove my safety choices. | #149 | L | Lifecycle database and live mobile-to-site checks passed. Native accept, decline, immediate reinvitation, both block scopes, explicit chat-leave selection, Team unblock, and sender unblock passed. The installed APK also passed text-label selection for the block scope and chat-leave checkbox. |
| 66 | As an authorized abuse reviewer, I want necessary invitation evidence held while a report is open, so that resend, unblock, or sender Account deletion cannot silently erase it. | #150 | H | Retention database checks passed 10/10. Live mobile-to-site cleanup and Room refresh passed. |
| 67 | As a Player, I want routine invitation history removed after its retention window without removing my profile or membership, so that invitation data does not define my continued participation. | #150 | H | Retention database checks passed 10/10. Live mobile-to-site cleanup and Room refresh passed. |

## Behavioral checks

| ID | Required behavior | Evidence sources | #153 result |
|---|---|---|---|
| 1 | A manager without an eligible Team creates one, optionally adds Players, returns to the same Event with that Team selected, and reaches review without final registration or charge. | S | Desktop, narrow site, and native creation, no-email Player addition, review, and explicit confirmation passed. |
| 2 | A returning manager skips creation and roster setup. Cover several eligible Teams with a remembered selection, one eligible fallback, several fallbacks, and no eligible Team. | S | Returning manager desktop, narrow, and Android review passed. The draft database suite covers remembered selection and each fallback. |
| 3 | The last-used preference changes only after successful Event registration in the same sport. Another sport and an abandoned draft do not overwrite it. | S | Passed in the 14-test draft database suite. |
| 4 | A site draft resumes on mobile and a mobile draft resumes on site. Preserve the selected Team and valid registration context. Recheck a removed Team, lost management authority, and unavailable Event. | S | Actual site-to-Android and Android-to-site resume passed. Each client restored the selected Team and reached explicit confirmation. Database authority and unavailable-selection checks passed. |
| 5 | A retry after a saved Team, saved Player, or failed delivery does not duplicate identities, roster entries, or invitation attempts. Closing and resuming does not resend invitations. | S, L | Database replay, lost-response, and concurrent creation checks passed. Actual UI resume passed in both client directions. |
| 6 | A new Player with email and a new Player without email each receive a durable User Profile without an Account. Each occupies one roster slot before acceptance and no additional Event Registration Capacity. | I, L | Managed-profile creation and lifecycle tests cover durable profiles without Accounts and roster capacity. Native no-email creation passed. |
| 7 | Pending roster presence does not grant Team access or chat access. Check direct API requests, not only hidden UI controls. | L | Lifecycle database checks passed, including denied Team access before acceptance. The live mobile-to-site suite passed. |
| 8 | Managers see complete roster status. Authorized hosts and officials see the permitted roster and readiness without invitation history. Regular members and public viewers do not receive unaccepted identities. | R, L | Privacy route checks passed for authorized and unauthorized viewers. The live operational roster test passed with Room refresh. |
| 9 | Adding a Player during one Event signup changes the canonical Team and current Event selection only. Another upcoming Event and completed participation history remain unchanged. | S | Passed in draft and cutover database checks. |
| 10 | Claim rejects missing authentication, missing confirmation, invalid or stale links, unverified attached email, and a profile claimed by another Account. Valid no-email URL claim succeeds only with authentication and confirmation. | I | Confirmed no-email and verified-email claims passed. Stale conversion links and concurrent claims passed in real database tests. |
| 11 | A verified, confirmed merge keeps the existing claimed profile primary. It preserves its personal details, roster and invitation associations, existing blocks, and history. Matching names alone do not merge profiles. | I | Persisted primary-profile and block preservation passed. Live mobile-to-site signing, imported evidence, merge, and Room refresh passed. |
| 12 | Contact correction on an unclaimed profile invalidates earlier links and records the correction. Managers cannot change verified contacts or claimed identities through this path. | I | Managed-profile and invitation-route tests cover contact correction, stale links, and claimed-identity restrictions. These checks passed in the full site run. |
| 13 | Selecting Is a minor requires date-only birthdate and guardian email. Siblings can share Guardian Contact without sharing User Profile identity or overwriting personal email. | G | Guardian tests cover date-only birthdate, required contact, and distinct sibling profiles. Native and browser fixtures preserve separate child IDs. |
| 14 | First guardian acceptance requires the named child, verified guardian contact, explicit declaration, and active relationship. A returning active guardian does not repeat setup. An unrelated Account cannot act for the child. | G | Persisted declaration and returning-authority checks passed. First-guardian browser acceptance and returning-guardian browser and Android acceptance passed. |
| 15 | Missing birthdate does not default to adult acceptance. A minor's Account does not bypass guardian acceptance. Test immediately before and at age 18, including family profile reads and edits. | G | The controlled-clock database check passed immediately before and at age 18. Missing-birthdate and minor-Account guards are covered by managed-profile and guardian tests. |
| 16 | Decline removes the current roster entry and frees capacity while retaining the outcome. Immediate reinvitation succeeds without a join-request requirement and creates a distinct attempt. | L | Lifecycle database checks passed. Native decline and immediate reinvitation passed with distinct retained attempts. |
| 17 | A reminder preserves the pending attempt identity and adds delivery history. Concurrent adds or resends do not create multiple current pending attempts. | L | Passed in the lifecycle database and live mobile-to-site suites. The full mobile test run passed. |
| 18 | Expiry retains roster placement, denies unaccepted Team access, and shows the expired manager state. A later invitation creates a new attempt. | L | Passed in the lifecycle database and live mobile-to-site suites. The full mobile test run passed. |
| 19 | Cancellation retains a terminal outcome. Old links and actions must not change a newer attempt or reintroduce a withdrawn roster assignment. | L | Passed in lifecycle and cutover database suites. |
| 20 | Block sender and Block Team enforce their different scopes. Cover a second manager on the same Team, the same sender on another Team, direct addition, reminders, and unblock. | L | Lifecycle database checks passed for both scopes. Native Team Block, sender block, and both unblock flows passed. |
| 21 | User Block rejects an unclaimed target in the API and UI. Guardian sender-block affects contact to that guardian. Guardian Team Block affects only the named child and Team. | L | Passed in the lifecycle database and live mobile-to-site suites. The full mobile test run passed. |
| 22 | Invitation sender-block leaves shared chats intact by default. Explicitly selecting Leave all chats with this user performs that action. Decline and block failure reports the actual saved state. | L | Both chat choices and complete rollback passed in the lifecycle database suite. |
| 23 | Unaccepted and Managed Players appear with missing required signatures. Accepted invitation alone does not satisfy documents. Valid Player, guardian, or approved imported evidence satisfies only the intended Version, Signer roles, and scope. | R | The live operational roster test passed with missing requirements, Player and guardian signing, approved imported evidence, profile merge, and Room refresh. |
| 24 | A controlled clock proves routine cleanup after 90 days from the final outcome. A new invitation or reminder cannot reset an older attempt's retention age. | H | Passed in the 10-test retention database suite. |
| 25 | An open abuse report preserves only needed held evidence through unblock, resend, and sender Account deletion. Closing the report releases the hold and uses the original retention window. | H | Passed in the 10-test retention database suite. |
| 26 | Invitation cleanup leaves the User Profile, current roster, and accepted Team Membership intact. Pending attempts are not removed as closed history. | H | Passed in the 10-test retention database suite and the live mobile-to-site retention and Room refresh test. |
| 27 | A failed related database write rolls back the full save. Concurrent claim, acceptance, decline, and reinvitation cannot transfer another Account's identity or let an obsolete action override the current attempt. | I, L, S | Related-write rollback, lifecycle races, and concurrent profile claims passed against PostgreSQL. |
| 28 | Existing-data conversion preserves known identities and valid associations. It does not merge matching names, create Accounts, or invent missing historical outcomes. | I | Passed in the nine-test cutover database suite. |

## Run record

The issue database accepted all 234 migrations. The conversion and claim tests use real PostgreSQL records. The latest cutover run passed 10/10, including current-age labels in both invitation views. The latest claim journey run passed 5/5. Draft checks passed 14/14. Retention checks passed 10/10. The combined privacy, lifecycle, and cutover run passed 63/63 before the additional label check.

The full site run completed 827 suites: 813 passed, one failed, and 13 skipped. It passed 5,970 tests. Two email tests failed because the run disabled outbound providers. The unchanged email suite passed 2/2 with its normal test environment. The final affected run passed all 170 tests in 17 suites, including database tests against the isolated database.

The full mobile run passed 1,917 tests and skipped 16 live-environment tests. Model passed 44/44. Network passed 109/109. Repository implementation passed 141/141. Compose passed 1,623 tests and skipped 16. Android lint and APK packaging passed. The four feature-specific mobile-to-site suites passed separately. The review found a pending-to-terminal refresh race. Both regression tests failed before the correction. The final full mobile test, lint, and APK run passed after both refresh paths were corrected.

### Native recipient and guardian outcomes

Android accepted the browser-claimed profile's Cascade invitation. It declined a River Crew invitation. Immediate reinvitation created a distinct attempt. Team Block saved and rejected a subsequent server invitation. Native Team unblock permitted a new attempt without restoring membership. Sender blocking also saved after explicit chat-leave selection. The checkbox was unchecked by default. Native Connections showed the sender block and removed it after confirmation. PostgreSQL retained all three distinct decline outcomes and their block scopes.

The final control APK passed text-label selection for both the sender radio control and the chat-leave checkbox. The accepted roster rows have no pending badge. Managed profile remains a separate label. The final production browser at 390 and 1280 pixels and the final Android APK show Awaiting guardian for the pending child.

The first guardian browser flow passed at 390 pixels. It required the guardian declaration and explicit Team acceptance. The returning guardian browser flow passed at 1280 pixels. It reused the active relationship and required explicit Team acceptance. Android then accepted Riley's Valley Juniors invitation through that active relationship. The saved actor and actingGuardianId identify the guardian. The userId still identifies Riley.

The narrow production browser completed a verified-email claim and separate Team acceptance. The merge preserved Taylor Brooks as the primary profile. The regular-member roster showed accepted Players and omitted the pending child.

### Platform limits

Direct native signed-link claim entry, including first-time guardian setup, was not tested. Automatic approval review blocked the earlier deep-link command. That command was not retried. Browser first-guardian setup, native returning-guardian acceptance, and persisted guardian authority checks passed. This Windows run provides no native iOS evidence.

The local Android Logout control reported that the device push token was unavailable. Firebase was not initialized in this local test build. The client retained the session because it could not complete push cleanup. Account switching used a reset of this test app's local data. Server records were preserved. Logout did not pass in this environment.

### Completion

The final production build, explicit TypeScript check, full mobile tests, Android lint, APK packaging, final guardian-label captures, and both review rechecks passed. The final live API recheck passed all four feature suites. It also confirmed the current invitation ID and label in Room after a normal Team refresh. Standards and Spec reviews have no remaining findings. Production conversion and deployment are separate operations. Neither was performed.

Final UI captures include final-guardian-label-390.png, final-roster-labels-1280.png, native-final-accepted-roster.png, native-final-guardian-label.png, and native-full-row-controls.png in the temporary evidence directory. Both client directions also have saved review and confirmation captures. All named final captures were inspected.
