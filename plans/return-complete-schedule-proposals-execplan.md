# Return Complete Schedule Proposals During Create

This ExecPlan is a living document. Update it as the implementation changes.

## Purpose / Big Picture

This living ExecPlan is the restartable runbook for the Issue #34 schedule-proposal path. A novice can use only this file to follow the user flow, identify the site and mobile seams, run the focused checks, and distinguish local evidence from current backend evidence. The unscheduled and scheduled Tournament live passes are current.

The user-visible outcome is that a person can choose League or Tournament during Create, edit the setup before Create, review a complete Schedule Proposal, accept it once, and reload the Event with the same Match Graph, placements, Fields, Time Slots, Resource rows, officiating assignments, End Policy, and Room-backed values. Rejecting or failing the proposal must preserve the draft and leave no partial Event or graph. The current scheduled live test proves the tested Tournament path through proposal acceptance, placement, persistence, and reload; local and generic checks cover the remaining boundaries.

The site owns the HTTP contract and scheduler. Mobile sends the command through `MvpApiClient`, keeps an unaccepted proposal outside Room, and writes accepted rows to Room in one transaction. A test case is a source test method or class. A test filter is the exact Gradle `--tests` selector. A live run is backend-backed execution after readiness succeeds. Local source tests, class filters, and Compose UI tests do not prove a live backend pass. The Tournament class now proves unscheduled `CREATE_ONLY` and scheduled `PROPOSED` acceptance with placements and reload.

## Progress

- [x] (2026-08-25 15:42Z) Read issue #37, parent issue #14, dependencies #31 and #33, repository rules, and the existing Event Editor paths.
- [x] (2026-08-25 15:42Z) Map the Event Editor contract, create operation receipt, scheduler, graph persistence, web submit flow, and mobile create flow.
- [x] (2026-08-25 15:42Z) Add proposal and accept contracts with revision bindings.
- [x] (2026-08-25 15:42Z) Persist pending proposals without persisting Events.
- [x] (2026-08-25 15:42Z) Accept the exact stored graph with atomic Event persistence.
- [x] (2026-08-25 15:42Z) Add site and mobile review and accept flows.
- [x] (2026-08-25 15:42Z) Add focused regression tests.
- [x] (2026-08-25 15:42Z) Run focused type checks and tests.
- [x] (2026-08-25 15:42Z) Run the complete site and mobile suites once.
- [x] (2026-08-25 16:25Z) Run the required two-axis code review.
- [x] (2026-08-25 16:25Z) Address the review finding for stale accepted-proposal replay and recover abandoned create-operation claims.
- [x] (2026-08-25 16:27Z) Commit the work on the current branch.
- [x] (2026-08-30 01:46Z) Document the separate Tournament mobile-to-site contract test and required Gradle filter that were added on 2026-08-29.
- [x] (2026-08-29 19:46:42.900Z) Historical evidence (not current post-refactor status): run the iOS Tournament parity test case `EventEditorTournamentParityCommonTest.given_shared_tournament_draft_when_command_is_encoded_then_complete_canonical_wire_is_preserved`; the result was one test, zero skipped, zero failures, and zero errors.
- [x] (2026-08-30 01:45:27.894Z) Historical evidence (not current post-refactor status): run the Android Tournament parity test case `EventEditorTournamentParityAndroidTest.given_shared_tournament_fixture_when_mobile_command_is_built_then_it_matches_the_web_golden`; the result was one test, zero skipped, zero failures, and zero errors.
- [x] (2026-08-30; historical class-filter record, no current XML artifact) The prior `EventEditorTournamentParityCommonTest` class-filter observation reported 4 tests with `skipped=0`, `failures=0`, and `errors=0`; the exact current iOS method and XML are recorded below.
- [x] (2026-08-30, current review-cycle record; execution timestamp not supplied) Record the Android combined scheduled and unscheduled parity selector: the exact methods `EventEditorTournamentParityAndroidTest.given_shared_tournament_fixture_when_mobile_command_is_built_then_it_matches_the_web_golden` and `EventEditorTournamentParityAndroidTest.given_scheduled_tournament_fixture_when_mobile_command_is_built_then_it_matches_the_web_golden` reported 2 tests with `skipped=0`, `failures=0`, and `errors=0`. This is local fixture evidence, not a live backend result.
- [x] (2026-08-30T07:28:29.010Z; historical XML report) The pre-fix `EventRepositoryRoomPersistenceTest` report recorded 7 tests with `skipped=0`, `failures=0`, and `errors=0`; the current 9-test Room result is reported generated evidence with its XML file unavailable in the current worktree.
- [x] (2026-08-30, current review-cycle record; execution timestamp not supplied) Record the current site `tournamentEditorParity.test.ts` exact file-filter result: one suite and 4 tests passed, with `skipped=0`, `failures=0`, and `errors=0`. This local result includes the scheduled case `matches the scheduled Tournament command and exact Match Demand`; it is not a backend live run.
- [x] (2026-08-29, historical readiness attempt) Record the standalone Tournament selector attempt under JDK 17 and the required environment. It failed in `@Before` at `EventLifecycleMobileApiIntegrationTest.kt:782` after the isolation probe reached loopback port `3010` and received HTTP 500 from `/api/app-version`; Gradle/JUnit reported `1 test completed, 1 failed`, `skipped=0`, `failures=1`, and `errors=0`. No create IDs existed, so cleanup was a no-op. This record is historical; it does not supersede the later unscheduled pass.
- [x] (2026-08-30T17:42:56.761Z; historical XML report) The earlier unscheduled `CREATE_ONLY` Tournament live pass used the exact Tournament selector and reported `tests=1`, `skipped=0`, `failures=0`, and `errors=0`; the later 19:03 XML report supersedes it.
- [x] (2026-08-30, current review-cycle record; execution timestamp not supplied) Current local cleanup evidence includes `apps/mobile/scripts/purge-mobile-event-editor-receipts.mjs`: it validates loopback and non-production database guards, while the integration harness supplies provider-disabled execution, waits for receipts to settle, runs `BEGIN`/`COMMIT`/`ROLLBACK`, deletes the exact operation-and-Event pair with `DELETE FROM "EventEditorCreateOperations" WHERE "createOperationId" = $1 AND "eventId" = $2`, and checks residual rows with `WHERE "createOperationId" = $1 OR "eventId" = $2`. `bash scripts/tests/mobile-cleanup-contract.sh` checks these contracts and runs the Node synthetic cases in `scripts/tests/mobile-cleanup-behavior.mjs`; its output includes `Mobile cleanup behavioral contract passed` and `Mobile build/dead-code cleanup contract passed`. The newest generic-selector attempt first passed the read-only readiness GET with `outboundProvidersDisabled=true` and a non-empty `databaseUrlHash` (hash value not recorded), then `MVP_TEST_DATABASE_URL="$(./scripts/ensure-local-backend.sh --print-database-url)"` failed before Gradle because the effective Compose `db` service was absent: the script could not read `POSTGRES_USER`, `POSTGRES_PASSWORD`, or `POSTGRES_DB`, and could not resolve the Compose db port; `docker compose --env-file .env.docker ps` showed no containers. Gradle ran without a usable `MVP_TEST_DATABASE_URL` and failed in `@Before` at `EventLifecycleMobileApiIntegrationTest.kt:815` with `Required mobile/backend contract test requires MVP_TEST_DATABASE_URL`; it reported `tests=1`, `skipped=0`, `failures=1`, and `errors=0`. No Event Editor request or receipt was created, cleanup and residual checks did not run, and no generic live pass or generic receipt-cleanup result is claimed.
- [x] (2026-08-30; historical review record; execution timestamp not supplied) The two-axis standards/specification review at fixed base `bd683fb` reported no actionable findings. It predates the current mobile graph identity/team and site playoff omission/explicit-`null` fixes; no post-fix clean review is claimed.
- [x] (2026-08-30T18:34:08.735Z; current directly inspectable XML report) The exact iOS parity method `EventEditorTournamentParityCommonTest.given_shared_tournament_draft_when_command_is_encoded_then_complete_canonical_wire_is_preserved` produced `tests=1`, `skipped=0`, `failures=0`, and `errors=0` in the directly inspectable XML file `apps/mobile/core/repository-impl/build/test-results/iosSimulatorArm64Test/TEST-iosSimulatorArm64Tes-FUG48HN0UTP9U.com.razumly.mvp.core.data.repositories.EventEditorTournamentParityCommonTest.xml`.
- [x] (2026-08-30; current source/test trace) At the Room seam, `EventRepository.saveEventEditor` decodes authoritative graph state and uses `EventCacheMerge.withRebuiltEditorGraphState`, `mergeRebuiltEditorGraphFields`, and `mergeRebuiltEditorGraphTimeSlots` before the Room transaction. `EventEditorSaveResultDto.matchesForPersistence` performs Event and Match identity validation before persistence. Validator map: the `PRESERVE` response path uses `EventEditorSaveResultDto.matchesForPersistence`; graph-bearing create/accept and `BUILT`/`REBUILT`/reconcile responses use `EventEditorCreateProposalGraphDto.decodeOrThrow`. The malformed graph tests `given_cross_event_match_in_save_response_when_room_persists_then_cache_is_unchanged` and `given_wrong_event_graph_without_matches_when_room_persists_then_cache_is_unchanged` use response `NOT_REQUESTED` plus command mode `PRESERVE`; the valid rebuilt test uses mode `REBUILT`; the valid accepted test uses create mode `PROPOSED` followed by accept mode `BUILT`. The reported 9-test generated Room XML file is unavailable in the current worktree.
- [x] (2026-08-30T19:03:28.014Z; reported generated XML evidence; file unavailable in current worktree) The unscheduled `CREATE_ONLY` Tournament live selector `--tests 'com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest.given_mobile_tournament_editor_create_command_when_sent_to_site_then_tournament_is_persisted'` produced a reported result with `tests=1`, `skipped=0`, `failures=0`, and `errors=0`; cleanup and residual checks passed. This is one unscheduled `CREATE_ONLY` saved graph only; all-mode identity is attributed to local Room tests.
- [x] (2026-08-30; current focused test observation) From `apps/site`, `npm test -- --runInBand src/server/repositories/__tests__/events.upsert.test.ts -t "playoff config"` produced one passed suite, 3 passed tests, 89 skipped tests, 0 failures, and 0 errors for playoff omission and explicit-`null` behavior.
- [x] (2026-08-30; final post-fix two-axis review; execution timestamp not supplied) Standards and specification re-review is clean for the Room graph, cleanup, and living plans. The three cleanup findings—generic teardown durable-receipt ownership, dispatched generic-receipt settlement, and in-flight Tournament purge—each moved `OPEN` -> `FIXED` -> `RE-REVIEWED` -> `VERIFIED`; `purge-mobile-event-editor-receipts.mjs` is staged. All plan findings are verified. The current backend-backed scheduled run below now supplies the previously pending `PROPOSED` acceptance and placement evidence; this runtime result does not claim a new review cycle.
- [x] (2026-08-30; newest generic-selector infrastructure-blocker record; current generated XML unavailable in the current worktree) Read-only GET `http://127.0.0.1:3100/api/app-version?platform=ANDROID&versionName=0.0.0&buildNumber=0&mvpTestIsolation=1` returned `outboundProvidersDisabled=true` and a non-empty `databaseUrlHash`; the hash value is not recorded. The exact generic selector was then attempted with the documented environment, but `MVP_TEST_DATABASE_URL="$(./scripts/ensure-local-backend.sh --print-database-url)"` failed before Gradle because the effective Compose `db` service was absent: the script could not read `POSTGRES_USER`, `POSTGRES_PASSWORD`, or `POSTGRES_DB`, and could not resolve the Compose db port; `docker compose --env-file .env.docker ps` showed no containers. Gradle ran without a usable `MVP_TEST_DATABASE_URL` and failed in `@Before` at `EventLifecycleMobileApiIntegrationTest.kt:815` with `Required mobile/backend contract test requires MVP_TEST_DATABASE_URL`; reported `tests=1`, `skipped=0`, `failures=1`, and `errors=0`. No Event Editor request or receipt was created, cleanup and residual checks did not run, and no generic live pass is claimed.
- [x] (2026-08-31T00:08:16.201Z; current backend-backed XML report) From `apps/mobile`, the exact class selector `--tests 'com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest'` ran after a private database-hash preflight against healthy `issue34-site-db` on loopback port `5543`. XML `apps/mobile/composeApp/build/test-results/testDebugUnitTest/TEST-com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest.xml` reports `tests=2`, `skipped=0`, `failures=0`, and `errors=0`; both the existing unscheduled and new scheduled test cases passed. The scheduled method `given_mobile_scheduled_tournament_create_when_proposed_and_accepted_then_placements_and_room_reload_persist` verified `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END`, `isAutomatedScheduling=true`, and `hasScheduleProposalSupport=true`, then verified the backend `PROPOSED` response and acceptance, placed Tournament Match Graph, persistence and reload, Room graph/resources, cleanup, and residual checks. Four daily windows and `gamesPerOpponent=1` supplied valid capacity while retaining pool/bracket demand. The prior `mvp-site-db:5433` hash mismatch is historical ownership-conflict evidence; this run does not establish that the current worktree's `--print-database-url` helper works.
- [x] (2026-08-31; current cleanup source/fixture trace) `apps/mobile/scripts/purge-mobile-tournament-test-run.mjs` classifies a terminal failed-create receipt only when create dispatch is terminal, acceptance dispatch has not started or terminated, the receipt event ID matches `SERVER_EVENT_ID_PATTERN`, `responseJson` and `proposalJson` are null, `proposalStatus` is `NONE`, and `emailDelivery` is `PROCESSING`. The synthetic `runTypedCreateRejectionPurge` fixture clears graph tables, uses an empty `resolvedEventId`, marks `createDispatchTerminal=true`, sets those receipt fields to null/`NONE`/`PROCESSING`, and verifies one commit with the operation receipt removed.
- [x] (2026-08-31T03:00:56.757Z; observed XML report) The Room class filter reports `tests=10`, `skipped=0`, `failures=0`, and `errors=0`, including accepted graph reload after a narrow detail response and graph replacement. The report remains in the repository module output.
- [x] (2026-08-31T03:00:29.165Z; observed generated XML report) The exact Tournament class selector reports `tests=2`, `skipped=0`, `failures=0`, and `errors=0`. Later Compose test filters replaced the shared report directory. The observed result covers unscheduled `CREATE_ONLY` and scheduled `PROPOSED` acceptance, one operation-bound acceptance PUT, `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END`, placed Match Graph, Room-first persistence, remote/editor reload, and cleanup.
- [x] (2026-08-31T03:02:08.506Z; current Android XML report) The two Android Tournament parity methods report `tests=2`, `skipped=0`, `failures=0`, and `errors=0`. The iOS Tournament parity target reports 4 tests with the same zero counts.
- [x] (2026-08-31T03:01:46Z; observed mobile setup reports) The three named Tournament setup methods each report one passing test with zero skipped, failures, or errors. The generic One-Time Event live selector also reports one passing test with zero skipped, failures, or errors; later Compose filters replaced that shared report directory.
- [x] (2026-08-31; current site and cleanup checks) Site scheduler, bracket, and parity tests report 3 suites and 22 tests passed. EventBuilder complexity lint exits 0. The site repository regression reports 1 suite and 92 tests passed, and TypeScript exits 0. Mobile cleanup behavior and contract checks pass.

## Milestones

M1 — shared contract, Tournament setup, and review-before-Create. First keep the version-3 Event Editor request and revision bindings identical across the site schema, mobile DTO, mapper, and HTTP client. Then follow the actual mobile sequence: selecting Tournament applies defaults; `Event.applyCreateSelectionRules` enforces team signup and mobile defaults; `simpleSetupSectionVisibility` exposes the division-oriented setup pages; and immediate editing updates the draft before Create. The exact existing local method `DefaultCreateEventComponentTest.given_immediate_tournament_edit_when_created_then_first_command_contains_final_values` checks Tournament type, single-division state, field count, name, organization, divisions, start/end, automated scheduling, fixed end, and double elimination in the first command. `CreateEventSelectionRulesTest.tournament_selection_enforces_team_signup_and_mobile_defaults` and `EventCreateSimpleSetupTest.given_tournament_setup_when_pages_are_resolved_then_division_rules_are_used_without_league_scoring` cover defaults and page resolution. These methods do not prove every rendered control or callback; Time Slots, Resource rows, officiating, and complete graph behavior are later evidence. Run named methods, not a broad UI class filter.

M2 — competition mapping, proposal computation, graph, placements, and End Policy. Carry both shared Tournament fixtures through `EventEditorSessionMapper.toCreateCommand`, the site parser, `eventEditorSave`, `EventBuilder`, `eventScheduleMutation`, and serialization. The unscheduled fixture must remain `isAutomatedScheduling=false` with `CREATE_ONLY`; the scheduled sibling must use `isAutomatedScheduling=true`, `FIXED_END`, end constraint `2026-10-03T20:00:00Z`, `CREATE_AND_BUILD_SCHEDULE`, and proposal support true. Both local cases must produce Match Demand total 38 (`POOL=24`, `BRACKET=14`, `placed=0`, `unplaced=38`) with distinct operation IDs. The site bracket matrix currently proves seven local scheduler cases and the EventBuilder complexity check exits 0; these are not a live backend result.

M3 — proposal transaction, accept/reject, retry, typed failures, Room, and cleanup. Store a pending proposal in the operation receipt without a durable Event. Accept only the stored operation ID and proposal revision, recheck authoritative revisions, and persist the exact stored graph in one transaction. Mobile keeps the proposal outside Room until acceptance, then `EventRepository` writes all accepted rows with `DatabaseService.withTransaction`. The current Room class-filter XML reports 10 tests with zero skipped, failures, or errors. Validator attribution is `PRESERVE` -> `EventEditorSaveResultDto.matchesForPersistence`; graph-bearing create/accept and `BUILT`/`REBUILT`/reconcile -> `EventEditorCreateProposalGraphDto.decodeOrThrow`; malformed guards are `NOT_REQUESTED`/`PRESERVE`, valid rebuilt is `REBUILT`, and valid accepted is create `PROPOSED` then accept `BUILT`. Cleanup remains bounded to run-owned receipts.

M4 — web/mobile parity, Room reload, and current live evidence. Compare `editorDraftToLegacyEvent`, site scheduler serialization, and `EventEditorSessionMapper` against both shared Tournament fixtures. The prior common parity class-filter observation of 4 tests is historical. Current iOS parity reports 4 tests, Android parity reports 2 tests, site parity reports one suite and 4 tests, Room reports 10 tests, and the Tournament live class reports two tests with zero skipped, failures, or errors. The live result covers unscheduled `CREATE_ONLY` and scheduled `PROPOSED` acceptance with placements, persistence, and reload.

## Surprises & Discoveries

- The current scheduled Create path computes and persists Event and Schedule data in `createEventEditor`; the proposal path must split computation from durable acceptance.
- The scheduler returns a complete in-memory Match Graph through `reconcileEventSchedule`, and `serializeMatches` includes placements, links, official assignments, Fields, and teams.
- The operation receipt supplies durable idempotency. A pending proposal can use the same identity without creating a durable Event until accept.
- Mobile must keep a proposed result outside Room. Only an accepted result may write Event, Phase, Division, Match, Schedule, Field, Time Slot, Resource, and assignment rows.
- A proposal review must show unavailable labels explicitly and format proposal times in the Event time zone.
- Accepted-proposal replay validates the submitted draft before returning the canonical result. Stale `PROCESSING` claims use a conditional lease reclaim.
- The standalone Tournament selector has a distinct backend-readiness gate. The earlier attempt used JDK 17 and the required environment, entered `@Before` at `EventLifecycleMobileApiIntegrationTest.kt:782`, reached loopback port `3010`, and received HTTP 500 from `/api/app-version`. Gradle/JUnit observed `1 test completed, 1 failed`, `skipped=0`, `failures=1`, and `errors=0`. No create IDs existed, so cleanup was a no-op. This is historical readiness evidence. The later standalone selector passed for unscheduled `CREATE_ONLY`; the 2026-08-31 class selector passed both unscheduled and scheduled methods, with the scheduled method now proving proposal acceptance, placements, and Room reload.
- Historical readiness note: The failed response was associated with missing `.next/dev/required-server-files.json` and React/react-dom `19.2.4` versus `19.2.8` warnings. Runtime repair or restart is not authorized by this documentation task. This note explains the earlier failure and does not change the current scheduled or unscheduled pass.
- Historical evidence: the prior common parity class filter reported 4 tests and the pre-fix Room XML reported 7 tests. Current XML evidence is recorded in Progress: Room reports 10 tests, the Tournament live class reports 2 tests, Android parity reports 2 tests, and iOS parity reports 4 tests. The historical counts and unavailable-artifact notes do not limit the current evidence.
- Current mobile `EventRepository.saveEventEditor` decodes authoritative `BUILT`/`REBUILT` graphs, calls `EventCacheMerge.withRebuiltEditorGraphState`, `mergeRebuiltEditorGraphFields`, and `mergeRebuiltEditorGraphTimeSlots`, then writes the event, graph-owned Fields, Time Slots, matches, and teams inside the Room transaction. The validator map is `PRESERVE` -> `EventEditorSaveResultDto.matchesForPersistence` and graph-bearing create/accept plus `BUILT`/`REBUILT`/reconcile -> `EventEditorCreateProposalGraphDto.decodeOrThrow`. The current Room class reports 10 passing tests.
- Current site `upsertEventFromPayload` evidence is in `apps/site/src/server/repositories/__tests__/events.upsert.test.ts`: the focused `-t "playoff config"` selector passed `clears an existing playoff config when the payload explicitly sends null`, `preserves an existing playoff config when a partial update omits it`, and `retains legacy playoff config fallback when playoffConfig is omitted`. It reported 3 passed tests, 89 skipped tests, 0 failures, and 0 errors.
- The cleanup-contract script has current local source and synthetic behavior evidence. It checks exact operation-and-Event deletion, OR residual checks, transaction guards, and integration wiring, then emits `Mobile cleanup behavioral contract passed` and `Mobile build/dead-code cleanup contract passed`. The generic One-Time Event live selector now also reports one passing test with zero skipped, failures, or errors. The earlier Compose URL derivation failure is historical.
- [x] (2026-08-31; current cleanup source/fixture trace) The Tournament cleanup helper `apps/mobile/scripts/purge-mobile-tournament-test-run.mjs` requires terminal create dispatch, no acceptance dispatch, a server-shaped receipt event ID, null response/proposal JSON, `proposalStatus=NONE`, and `emailDelivery=PROCESSING` for a terminal failed-create receipt. Its `runTypedCreateRejectionPurge` synthetic fixture supplies the empty resolved event ID, terminal create flag, cleared graph tables, and null/`NONE`/`PROCESSING` receipt fields, then verifies commit and receipt deletion.
- Database provenance distinction: the current scheduled run used a private hash preflight against healthy `issue34-site-db` on loopback port `5543`. The prior `mvp-site-db:5433` hash mismatch was an ownership conflict, not a current source defect, and the worktree's `--print-database-url` helper is not claimed to work.
- The exact immediate-edit method covers Tournament selection, field count, `updateTournamentField`, name, organization, divisions, start/end, automated scheduling, fixed end, and double elimination. It does not prove Time Slot selection, Resource rows, officiating, or every rendered Tournament callback.
- Recovery trigger: after an authorized backend repair or restart, rerun the provider-disabled launcher and readiness curl, derive the guarded database URL, and rerun the generic One-Time Event selector. Record a new generic pass only when the XML reports one test with zero skips, failures, or errors and the persisted reload assertions pass; do not treat the scheduled pass as evidence that the current worktree's Compose URL derivation works. The scheduled class evidence is current. The cleanup-contract check currently passes its behavior and build/dead-code checks; rerun its exact script after future implementation changes and record only observed output.

## Decision Log

- Decision: Keep the Event Editor contract version at 3. Add a discriminated `PROPOSED` response and an accept command. Existing immediate Create responses remain `SAVED`.
  Rationale: The new response is additive and keeps installed mobile clients from receiving a different command shape. The mobile gateway can decode the response discriminator.
  Date/Author: 2026-08-24 / Codex.

- Decision: Store the canonical pending proposal in `EventEditorCreateOperations` and reserve its Event ID. Do not create a durable Event during proposal computation.
  Rationale: The existing operation receipt gives retries one durable identity. The scheduler requires an Event row, so proposal computation uses a rollback-only transaction. The transaction returns the serialized graph through a sentinel rollback, and no Event row remains.
  Date/Author: 2026-08-24 / Codex.

- Decision: Accept only by operation ID and proposal revision. Do not accept a client-supplied graph.
  Rationale: The server must persist the exact reviewed graph and must reject client recomputation or graph tampering.
  Date/Author: 2026-08-24 / Codex.

- Decision: Bind selected Fields, Time Slots, Rental Booking rows, and field availability to stable hashes of authoritative rows and blocker intervals.
  Rationale: `updatedAt` alone does not detect all availability changes. A canonical blocker hash detects a new or changed placement conflict before accept.
  Date/Author: 2026-08-24 / Codex.
- Decision: Keep the separate Issue #34 Tournament mobile-to-site check in this schedule-proposal plan as well as in the cross-application contract plan.
  Rationale: It exercises the same schedule-proposal HTTP seam (`MvpApiClient` -> `POST /api/events/editor`) and supplies shared acceptance evidence for the complete Tournament graph, Resource and Time Slot selections, fixed end policy, and Room/editor reload. The cross-application plan owns generic boundary compatibility; this plan owns schedule-proposal semantics, so the check belongs in both plans. The current class result covers unscheduled `CREATE_ONLY` and scheduled `PROPOSED` acceptance with placements.
  Date/Author: 2026-08-30 01:46Z / Codex.

## Review

The recorded post-fix two-axis review remains clean for the Room graph, cleanup, and living plans. Direct final checks now confirm the scheduled and generic live selectors, setup tests, Android and iOS parity, the 10-test Room class, site scheduler and persistence regressions, TypeScript, and cleanup behavior. No database credential or hash is recorded.

## Context and Orientation

The backend source of truth is under `apps/site`. The strict Event Editor contract is `apps/site/src/contracts/eventEditor.ts`. The create operation receipt is `apps/site/src/server/events/eventCreateOperationReplay.ts`. The create transaction is `apps/site/src/server/events/eventEditorSave.ts`. The Event Editor route is `apps/site/src/app/api/events/editor/route.ts`. The scheduler and graph persistence are under `apps/site/src/server/scheduler/eventScheduleMutation.ts` and `apps/site/src/server/repositories/events.ts`.

The site create screen is `apps/site/src/app/events/[id]/schedule/page.tsx`. The shared mobile DTOs are under `apps/mobile/core/network/src/commonMain/kotlin/com/razumly/mvp/core/network/dto/EventEditorDtos.kt`. The mobile HTTP gateway is `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventEditorRemoteGateway.kt`. The Room write path is `EventRepository.createEventEditor`.

In this plan, a discriminated response is a JSON response whose `status` selects one complete shape, either `SAVED` or `PROPOSED`; the parser is `eventEditorCreateResultSchema`. An operation receipt is the durable `EventEditorCreateOperations` row keyed by `createOperationId`; it stores the request hash and canonical result so retries do not recompute or duplicate effects. A rollback-only transaction is a database transaction used to build a proposal while deliberately discarding temporary Event and graph rows. A sentinel rollback is the private exception that carries the serialized proposal out of that transaction so the caller can return it after the database rolls back.

## Context Boundary

Start with `PLANS.md`, this plan, and the Issue #34 acceptance criteria. This is the minimum source set for the complete schedule-proposal plan, not only for the Tournament live run. In this plan, a test case is a source test method or class, a test filter is the exact Gradle `--tests` argument that selects it, and a live run is backend-backed execution after readiness succeeds.

- `docs/agents/issue-tracker.md`, `AGENTS.md`, `apps/site/CODING_STANDARDS.md`, and `apps/mobile/AGENTS.md` contain the issue-planning, execution-plan, and cross-application coding rules.
- `apps/site/src/contracts/eventEditor.ts`, `apps/site/src/contracts/appVersion.ts`, `apps/site/src/app/api/events/editor/route.ts`, `apps/site/src/app/api/events/[eventId]/editor/route.ts`, and `apps/site/src/app/api/app-version/route.ts` define the proposal, accept, reject, and readiness request/response shapes and parsers.
- `apps/site/src/server/events/eventEditorSave.ts`, `apps/site/src/server/events/eventCreateOperationReplay.ts`, and `apps/site/src/server/events/eventEditorWireCompatibility.ts` own validation, proposal computation, accept/reject transactions, revision binding, operation receipts, and wire compatibility.
- `apps/site/src/server/scheduler/eventScheduleMutation.ts`, `apps/site/src/server/scheduler/EventBuilder.ts`, `apps/site/src/server/scheduler/serialize.ts`, `apps/site/src/server/scheduler/officialStaffing.ts`, `apps/site/src/server/scheduler/matchGraph.ts`, and `apps/site/src/server/repositories/events.ts` own Match Graph construction, placement, officiating, serialization, and Event/graph persistence.
- `apps/site/prisma/schema.prisma` and `apps/site/prisma/migrations/20260824120000_add_event_editor_schedule_proposals/migration.sql` define the durable proposal and operation-receipt storage.
- `apps/site/src/app/events/[id]/schedule/page.tsx`, `apps/site/src/app/events/[id]/schedule/proposalDisplay.ts`, `apps/site/src/app/events/[id]/schedule/__tests__/proposalDisplay.test.ts`, `apps/site/src/app/events/[id]/schedule/components/eventForm/editorContractAdapters.ts`, and `apps/site/src/app/events/[id]/schedule/schedulePage/useCreateEventFlow.ts` own web proposal display, draft retention, and create-flow submission.
- `apps/mobile/core/network/src/commonMain/kotlin/com/razumly/mvp/core/network/dto/EventEditorDtos.kt`, `apps/mobile/core/network/src/commonMain/kotlin/com/razumly/mvp/core/network/dto/AppVersionDtos.kt`, and `apps/mobile/core/network/src/commonMain/kotlin/com/razumly/mvp/core/network/MvpApiClient.kt` define the mobile proposal/accept/reject DTOs, JSON encoding, and HTTP client.
- `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventEditorRemoteGateway.kt`, `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventRepository.kt`, `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventRepositoryContract.kt`, `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventRepositoryModels.kt`, `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventEditorSessionMapper.kt`, `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventCacheMerge.kt`, and `apps/mobile/core/repository-impl/src/commonMain/kotlin/com/razumly/mvp/core/data/repositories/EventRoomStore.kt` own mobile proposal decoding, accept/reject calls, retry identity, and Room persistence/merge behavior.
- `apps/mobile/core/database/src/commonMain/kotlin/com/razumly/mvp/core/data/DatabaseService.kt`, `apps/mobile/core/database/src/commonMain/kotlin/com/razumly/mvp/core/db/MVPDatabaseService.kt`, `apps/mobile/core/database/src/commonMain/kotlin/com/razumly/mvp/core/data/dataTypes/daos/EventDao.kt`, `apps/mobile/core/database/src/commonMain/kotlin/com/razumly/mvp/core/data/dataTypes/daos/MatchDao.kt`, `apps/mobile/core/database/src/commonMain/kotlin/com/razumly/mvp/core/data/dataTypes/daos/FieldDao.kt`, and `apps/mobile/core/database/src/commonMain/kotlin/com/razumly/mvp/core/data/dataTypes/daos/EventTimeSlotDao.kt` define the Room transaction and row writers used to persist accepted Event, Phase, Division, Match Graph, Schedule, Resource, and officiating data.
- `apps/mobile/composeApp/src/commonMain/kotlin/com/razumly/mvp/eventCreate/DefaultCreateEventComponent.kt` owns mobile defaults, immediate edit state, validation, end-policy normalization, retry identity, typed failure presentation, proposal acceptance/rejection, and pending proposal state.
- `apps/mobile/composeApp/src/commonMain/kotlin/com/razumly/mvp/eventCreate/CreateEventScreen.kt` owns the mobile proposal dialog and create-flow controls, including `ScheduleProposalDialog`.
- `apps/mobile/composeApp/src/androidUnitTest/kotlin/com/razumly/mvp/eventCreate/ScheduleProposalDialogUiTest.kt`, `apps/mobile/composeApp/src/commonTest/kotlin/com/razumly/mvp/eventCreate/DefaultCreateEventComponentTest.kt`, and `apps/mobile/core/repository-impl/src/commonTest/kotlin/com/razumly/mvp/core/data/repositories/EventEditorRemoteGatewayTest.kt` provide proposal UI, repository, accept/reject, wire, and typed-error evidence. The remote-gateway methods are `given_create_command_when_sent_then_wire_tree_contains_required_rows`, `given_schedule_proposal_when_created_then_proposal_graph_and_revision_binding_are_decoded`, and `given_proposal_when_accepted_and_rejected_then_wire_bodies_match_contract`. The obsolete HTTP test reference is not part of this plan.
- `apps/site/src/server/events/__tests__/eventEditorSave.test.ts`, `apps/site/src/server/events/__tests__/eventCreateOperationReplay.test.ts`, `apps/site/src/app/api/events/__tests__/editorContractRoutes.test.ts`, `apps/site/src/server/scheduler/__tests__/serialize.test.ts`, `apps/site/src/server/scheduler/__tests__/tournamentBracketMatrix.test.ts`, and `apps/site/src/server/scheduler/__tests__/tournamentEditorParity.test.ts` provide focused contract, replay, scheduler, and parity evidence.
- `test-fixtures/event-editor/tournament-parity-draft.json`, `test-fixtures/event-editor/tournament-parity-web-golden.json`, and `test-fixtures/event-editor/tournament-parity-scheduled-web-golden.json` are the shared unscheduled and scheduled parity fixtures. The scheduled sibling is required when checking `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END`, and the 38-match demand.
- `apps/mobile/composeApp/src/androidUnitTest/kotlin/com/razumly/mvp/eventDetail/EventLifecycleMobileApiIntegrationTest.kt`, `apps/mobile/composeApp/src/androidUnitTest/kotlin/com/razumly/mvp/testing/MobileApiIntegrationSupport.kt`, `apps/mobile/composeApp/build.gradle.kts`, and `apps/mobile/core/repository-impl/build.gradle.kts` define the live Tournament test case, backend gate, test filters, and parity task wiring.
- `apps/mobile/scripts/ensure-local-backend.sh`, `apps/mobile/scripts/purge-mobile-tournament-test-run.mjs`, `apps/mobile/scripts/purge-mobile-event-editor-receipts.mjs`, `apps/mobile/scripts/tests/mobile-cleanup-contract.sh`, `apps/mobile/scripts/tests/mobile-cleanup-behavior.mjs`, `apps/site/src/contracts/appVersion.ts`, and `apps/site/src/app/api/app-version/route.ts` define the provider-disabled launcher, guarded database URL, cleanup guards, receipt cleanup, synthetic cleanup behavior, and readiness response.
- Current generic live evidence was observed in generated XML immediately after the run: the exact One-Time Event selector reports `tests=1`, `skipped=0`, `failures=0`, and `errors=0` after the provider-disabled readiness probe and private database-target check passed. Later Compose filters replaced the shared report directory. The earlier Compose URL derivation failure remains historical.

### Optional expansion triggers

Do not read more sources unless a trigger fires. Expand the source set only for one of these exact conditions:

- Contract/schema mismatch: a request or response field, contract version, or parser rule differs between the site schema and a mobile DTO, encoder, mapper, or caller.
- Fixture drift: the Issue #34 fixture, web golden, seeded identifiers, Match Graph shape, selected resources, or fixed-end value changes.
- Launcher/readiness failure: the backend launcher, database guard, or `/api/app-version` readiness request fails or reports a different result.
- Changed endpoint: the mobile-to-site method, path, host port, or readiness endpoint changes.
- Test evidence gap: the focused Gradle command cannot select the intended case, or its output does not show the complete graph, selected resources, fixed-end policy, and reload result.

When a trigger fires, read the changed source and its direct test or caller. Record the reason in `Surprises & Discoveries` and update every affected command, assertion, and acceptance statement.

## Plan of Work

Follow this sequence for a clean Issue #34 implementation or restart. Keep the site as the contract and scheduler owner, and use mobile tests to prove mapping and Room behavior without importing site types.

First, establish the version-3 Event Editor seam. Keep `expectedRevisions` required in `apps/site/src/contracts/eventEditor.ts`, copy the create bootstrap revisions in `EventEditorSessionMapper.toCreateCommand`, and ensure the mobile DTO encoder and `MvpApiClient` send the same fields to `POST /api/events/editor`. Focused contract and mapper cases must assert all three revision keys, including JSON `null` for a missing staff revision.

Next, implement M1 mobile setup and review-before-Create. `DefaultCreateEventComponent.onTypeSelected` applies Tournament defaults, `Event.applyCreateSelectionRules` normalizes them, `EventCreateSimpleSetup.simpleSetupSectionVisibility` selects the relevant pages, and `CreateEventScreen`/`EventDetails` route edit callbacks. Verify the exact `DefaultCreateEventComponentTest.given_immediate_tournament_edit_when_created_then_first_command_contains_final_values` method plus the named selection-rules and setup-page methods. Record only the controls those methods actually assert; do not use the broad `EventLifecycleMobileApiIntegrationTest` class as M1 control proof.

Then, implement M2 proposal computation for both fixture modes. Validate the draft and authoritative revisions, map the competition configuration, build pool and bracket graph demand through `EventBuilder`, place matches and resources, serialize Fields, Time Slots, and officials, and normalize fixed/generated end constraints. The unscheduled fixture must produce `CREATE_ONLY` with automated scheduling false; the scheduled sibling must produce `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END` with end constraint `2026-10-03T20:00:00Z`, and proposal support true. The exact site scheduled case `matches the scheduled Tournament command and exact Match Demand` and the mobile method `EventEditorTournamentParityCommonTest.given_scheduled_tournament_draft_when_command_is_encoded_then_proposal_wire_is_preserved` must each observe Match Demand total 38 (`POOL=24`, `BRACKET=14`, `placed=0`, `unplaced=38`). Invalid input must fail before a durable Event or graph exists. These are local source/parity checks, not a backend live result.

Then, implement M3 durable proposal and atomic acceptance. Persist the canonical proposal in `EventEditorCreateOperations` while rolling back temporary graph rows. Accept by operation ID and proposal revision only; recheck revisions, persist the stored graph without rescheduling, and run post-commit effects once. On mobile, decode `PROPOSED`, keep it outside Room, and write accepted rows through one Room transaction. Preserve operation identity on unchanged retries, reject changed payloads, retain visible values after typed failures, and clean only run-owned rows.

Finally, implement M4 parity and prove the seam. Run the exact iOS parity method, treat the prior common class-filter observation of 4 tests as historical, run the Android scheduled+unscheduled selector (2 tests), the exact site parity file filter (one suite and 4 tests), and the Room class filter (10 tests). The scheduled sibling asserts `FIXED_END`, `CREATE_AND_BUILD_SCHEDULE`, and Match Demand total 38; the unscheduled fixture asserts `CREATE_ONLY` and the same demand. The backend-backed Tournament class now proves both live paths: unscheduled `CREATE_ONLY` and scheduled `PROPOSED` acceptance with placements and reload. Run the cleanup contract separately.

## Concrete Steps

Run site commands from `apps/site` and mobile commands from `apps/mobile`.

1. Run focused site contract, proposal, scheduler, web, and type checks from `apps/site`:

       npm test -- --runInBand --runTestsByPath \
       src/contracts/__tests__/eventEditor.test.ts \
       src/app/api/events/__tests__/editorContractRoutes.test.ts \
       src/server/events/__tests__/eventEditorSave.test.ts \
       src/server/events/__tests__/eventCreateOperationReplay.test.ts \
       src/server/events/__tests__/eventEditorWireCompatibility.test.ts \
       src/server/scheduler/__tests__/serialize.test.ts \
       src/server/scheduler/__tests__/tournamentBracketMatrix.test.ts \
       src/server/scheduler/__tests__/tournamentEditorParity.test.ts \
       'src/app/events/[id]/schedule/__tests__/proposalDisplay.test.ts' \
       'src/app/events/[id]/schedule/components/eventForm/__tests__/eventEditorRoundTrip.test.ts'
       npx tsc --noEmit

   Jest must report the selected contract, proposal, scheduler, and web suites as passed. `npx tsc --noEmit` must exit 0 with no output. These are local source checks, not a backend live run.
2. Run the criterion-specific M1 mobile methods:

       ./gradlew --no-daemon :composeApp:testDebugUnitTest \
       --tests 'com.razumly.mvp.eventCreate.DefaultCreateEventComponentTest.given_immediate_tournament_edit_when_created_then_first_command_contains_final_values'
       ./gradlew --no-daemon :composeApp:testDebugUnitTest \
       --tests 'com.razumly.mvp.eventCreate.CreateEventSelectionRulesTest.tournament_selection_enforces_team_signup_and_mobile_defaults'
       ./gradlew --no-daemon :composeApp:testDebugUnitTest \
       --tests 'com.razumly.mvp.eventCreate.EventCreateSimpleSetupTest.given_tournament_setup_when_pages_are_resolved_then_division_rules_are_used_without_league_scoring'

   Observe the edited command fields and Tournament setup pages. These methods do not prove Time Slot, Resource, officiating, or every UI callback.

3. Run parity and Room checks as separate dependency-project filters:

       ./gradlew --no-daemon :core:repository-impl:iosSimulatorArm64Test \
       --tests 'com.razumly.mvp.core.data.repositories.EventEditorTournamentParityCommonTest.given_shared_tournament_draft_when_command_is_encoded_then_complete_canonical_wire_is_preserved'

       ./gradlew --no-daemon :core:repository-impl:testDebugUnitTest \
       --tests 'com.razumly.mvp.core.data.repositories.EventEditorTournamentParityCommonTest.given_scheduled_tournament_draft_when_command_is_encoded_then_proposal_wire_is_preserved'

       ./gradlew --no-daemon :core:repository-impl:testDebugUnitTest \
       --tests 'com.razumly.mvp.core.data.repositories.EventEditorTournamentParityAndroidTest.given_shared_tournament_fixture_when_mobile_command_is_built_then_it_matches_the_web_golden' \
       --tests 'com.razumly.mvp.core.data.repositories.EventEditorTournamentParityAndroidTest.given_scheduled_tournament_fixture_when_mobile_command_is_built_then_it_matches_the_web_golden'

       ./gradlew --no-daemon :core:repository-impl:testDebugUnitTest \
       --tests 'com.razumly.mvp.core.data.repositories.EventRepositoryRoomPersistenceTest'

       ./gradlew --no-daemon :core:repository-impl:testDebugUnitTest \
       --tests 'com.razumly.mvp.core.data.repositories.EventEditorRemoteGatewayTest.given_create_command_when_sent_then_wire_tree_contains_required_rows' \
       --tests 'com.razumly.mvp.core.data.repositories.EventEditorRemoteGatewayTest.given_schedule_proposal_when_created_then_proposal_graph_and_revision_binding_are_decoded'

   These MockEngine checks prove mobile wire encoding and proposal decoding. They do not prove a live backend.

   The prior common parity class-filter observation reported 4 tests and is historical. Current iOS parity reports 4 tests, Android parity reports 2 tests, and the Room class filter reports 10 tests with zero skipped, failures, or errors. The scheduled and unscheduled fixtures assert their respective scheduling modes and Match Demand total 38. The current Tournament live class reports two passing tests and supplies the backend scheduled acceptance and placement evidence.
- Artifact availability: the latest Room XML remains in the repository module output. Generic and Tournament XML reports were observed immediately after their runs and later replaced by other Compose test filters. The iOS parity XML is in the simulator test output. No credential, database URL, or hash value is recorded.

4. Run the exact proposal and web checks from `apps/site`:

       npm test -- --runInBand --runTestsByPath \
       src/app/api/events/__tests__/editorContractRoutes.test.ts \
       src/server/events/__tests__/eventEditorSave.test.ts \
       src/server/events/__tests__/eventCreateOperationReplay.test.ts \
       src/server/events/__tests__/eventEditorWireCompatibility.test.ts \
       src/server/scheduler/__tests__/serialize.test.ts \
       'src/app/events/[id]/schedule/__tests__/proposalDisplay.test.ts' \
       'src/app/events/[id]/schedule/components/eventForm/__tests__/eventEditorRoundTrip.test.ts'

   Also run the exact scheduled site test case from `apps/site`:

       npm test -- --runInBand src/server/scheduler/__tests__/tournamentEditorParity.test.ts \
       -t 'matches the scheduled Tournament command and exact Match Demand'

   Jest must report the selected suites as passed. Their assertions must cover `PROPOSED`, accept and reject, stale revisions, rollback, serialized graph data, web proposal display, and draft retention. The site parity file filter currently reports one suite and 4 tests with zero skipped/failures/errors; its scheduled test selector is `-t 'matches the scheduled Tournament command and exact Match Demand'`. These checks do not prove a backend live run.
5. For a future authorized live attempt, from `apps/mobile` launch and verify the provider-disabled non-production backend:

       MVP_TEST_DISABLE_OUTBOUND_PROVIDERS=1 MVP_BACKEND_PORT=3100 ./scripts/ensure-local-backend.sh
       database_url="$(./scripts/ensure-local-backend.sh --print-database-url)"
       database_url_hash="$(printf '%s' "$database_url" | shasum -a 256 | awk '{print $1}')"
       curl -fsS 'http://127.0.0.1:3100/api/app-version?platform=ANDROID&versionName=0.0.0&buildNumber=0&mvpTestIsolation=1' |
       EXPECTED_DATABASE_URL_HASH="$database_url_hash" node -e '
         let body = "";
         process.stdin.setEncoding("utf8");
         process.stdin.on("data", (chunk) => { body += chunk; });
         process.stdin.on("end", () => {
           const payload = JSON.parse(body);
           if (
             payload?.outboundProvidersDisabled !== true ||
             payload?.databaseUrlHash !== process.env.EXPECTED_DATABASE_URL_HASH
           ) process.exit(1);
         });
       '

   The launcher must complete. This readiness gate is not an ordinary app-version check: the request must include `mvpTestIsolation=1`, the payload must contain `outboundProvidersDisabled=true`, and `databaseUrlHash` must equal the SHA-256 hash of the guarded `database_url` that will be passed as `MVP_TEST_DATABASE_URL`. The `--print-database-url` command only derives and prints that guarded URL; it does not start or verify the backend.

6. Run the generic One-Time Event live case, then the standalone Tournament case, with the required provider-disabled environment, `MVP_TEST_ALLOW_DB_SEED=1`, `MVP_TEST_REQUIRE_BACKEND=1`, and JDK 17:

   Run the generic One-Time Event selector first, then the standalone Tournament selector. Current backend evidence records one passing generic test and a two-test Tournament class result. The reproducible command below still requires the guarded database URL; the earlier helper derivation failure is historical.
       MVP_TEST_DISABLE_OUTBOUND_PROVIDERS=1 \
       MVP_TEST_BACKEND_URL=http://127.0.0.1:3100 \
       MVP_TEST_DATABASE_URL="$(./scripts/ensure-local-backend.sh --print-database-url)" \
       MVP_TEST_ALLOW_DB_SEED=1 \
       MVP_TEST_REQUIRE_BACKEND=1 \
       JAVA_TOOL_OPTIONS='-XX:TieredStopAtLevel=1' \
       ./gradlew --no-daemon :composeApp:testDebugUnitTest \
       --rerun-tasks \
       --tests 'com.razumly.mvp.eventDetail.MobileEventEditorApiContractTest.given_mobile_editor_create_command_when_sent_to_site_then_event_is_persisted' \
       --stacktrace

  Historical attempt: readiness passed provider-disabled state, but `ensure-local-backend.sh --print-database-url` could not derive the URL because the effective Compose `db` service was absent. Gradle then failed in `@Before` without a usable `MVP_TEST_DATABASE_URL`; no Event Editor request or receipt was created. The current generic pass used the active backend database target privately and is recorded in Progress.

       MVP_TEST_DISABLE_OUTBOUND_PROVIDERS=1 \
       MVP_TEST_BACKEND_URL=http://127.0.0.1:3100 \
       MVP_TEST_DATABASE_URL="$(./scripts/ensure-local-backend.sh --print-database-url)" \
       MVP_TEST_ALLOW_DB_SEED=1 \
       MVP_TEST_REQUIRE_BACKEND=1 \
       JAVA_TOOL_OPTIONS='-XX:TieredStopAtLevel=1' \
       ./gradlew --no-daemon :composeApp:testDebugUnitTest \
       --rerun-tasks \
       --tests 'com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest.given_mobile_tournament_editor_create_command_when_sent_to_site_then_tournament_is_persisted' \
       --stacktrace

  A passing Tournament result is exactly an executed test with `skipped=0`, `failures=0`, and `errors=0`, plus a persisted Tournament after mobile reload. The current class XML at `2026-08-31T03:00:29.165Z` reports `tests=2`, `skipped=0`, `failures=0`, and `errors=0` for selector `--tests 'com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest'`. It covers the unscheduled `CREATE_ONLY` and scheduled `PROPOSED` methods, acceptance, placed graph, Room/editor reload, and cleanup.

7. Run the cleanup contract from `apps/mobile`:

       bash scripts/tests/mobile-cleanup-contract.sh

  Its current result is a pass: `bash scripts/tests/mobile-cleanup-contract.sh` and the Node behavior check emitted `Mobile cleanup behavioral contract passed` and `Mobile build/dead-code cleanup contract passed`. The generic One-Time Event selector also passed with one test and zero skipped, failures, or errors. The Tournament selector passed separately with the scheduled acceptance and placement checks.
- The earlier generic-selector infrastructure failure is historical. It produced no Event Editor request or receipt. Current generic live evidence is recorded in Progress.

8. Run the exact site scheduler and parity checks from `apps/site`:

       npm test -- --runInBand \
       src/server/scheduler/__tests__/serialize.test.ts \
       src/server/scheduler/__tests__/tournamentBracketMatrix.test.ts \
       src/server/scheduler/__tests__/tournamentEditorParity.test.ts
       npm run lint:complexity -- src/server/scheduler/EventBuilder.ts

   Jest reports 3 passed suites and 22 passed tests for scheduler, bracket, and parity checks. EventBuilder complexity lint exits 0. The site repository regression reports 1 suite and 92 passed tests. TypeScript exits 0. Mobile iOS, Android, Room, generic, and Tournament XML reports were observed after their runs; later Compose filters replaced the shared generic and Tournament reports. Historical class counts remain historical.
- Artifact availability: the latest Room XML remains in the repository module output. Generic and Tournament XML reports were observed immediately after their runs and later replaced by other Compose test filters. The iOS parity XML is in the simulator test output. No credential, database URL, or hash value is recorded.

## Validation and Acceptance

A scheduled League or Tournament POST returns `202` and a `PROPOSED` payload with the complete graph, placements, official assignments, source snapshot, proposal revision, and authoritative revision bindings. Accepting the unchanged proposal returns `201`, persists one Event and all related rows atomically, and does not call the scheduler again. Repeating accept returns the canonical result without a second Event.

Changing the Event Editor source, selected Field, Rental Booking, Time Slot, or conflicting availability returns a typed stale/proposal-invalid error and leaves the Event and graph absent. Rejecting a proposal leaves no Event or graph. Failed computation emits no post-commit effect. Web and mobile retain the submitted draft after failure. Mobile shows a proposal before Room contains the Event and writes accepted rows only after accept.

M1 local acceptance uses the named existing methods, especially `DefaultCreateEventComponentTest.given_immediate_tournament_edit_when_created_then_first_command_contains_final_values`. It proves only the listed Tournament fields and callbacks. It does not prove every control, Time Slot/Resource/officiating input, or full graph behavior.

M2 acceptance requires equivalent web/mobile competition mapping, complete pool/bracket graph demand, placements, selected Fields and Time Slots, officiating input, and fixed/generated End Policy, plus typed rejection before persistence for invalid setup. Local scheduler and parity tests are necessary but do not establish the backend live row.

M3 acceptance requires unchanged retry identity, changed-command rejection, visible draft preservation, atomic server and Room writes, and no partial rows. The current Room class XML reports 10 tests with zero skipped, failures, or errors and includes graph identity replacement, accepted graph persistence, narrow GET graph preservation, and malformed response guards. Validator attribution is `PRESERVE` -> `EventEditorSaveResultDto.matchesForPersistence`; graph-bearing create/accept and `BUILT`/`REBUILT`/reconcile -> `EventEditorCreateProposalGraphDto.decodeOrThrow`; malformed guards are `NOT_REQUESTED`/`PRESERVE`, valid rebuilt is `REBUILT`, and valid accepted is create `PROPOSED` then accept `BUILT`. The cleanup-contract check passes its local behavior and build/dead-code checks.

M4 live acceptance is current for both tested Tournament modes. The Tournament class used the real mobile `MvpApiClient` -> `POST /api/events/editor` path. Its current XML at `2026-08-31T03:00:29.165Z` reports two tests with zero skipped, failures, or errors. The scheduled method proves `PROPOSED` response, one operation-bound acceptance PUT, acceptance, `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END`, placed Match Graph, persistence, Room-first reload, editor reload, and cleanup.

The earlier JDK 17 readiness failure is historical. The current scheduled acceptance and placement result is recorded above.

## Idempotence and Recovery

The operation ID is the stable retry key. A proposal POST with the same command returns the stored proposal; the same ID with changed input returns a typed payload mismatch. Accept uses the stored proposal hash and ID. Proposal computation may roll back temporary Event/graph rows, and only accept may create durable rows. Mobile keeps pending proposals outside Room and writes accepted data in one transaction.

The local setup can be repeated against the same non-production backend when the provider-disabled launcher and guarded URL are used. Never point the test at production. If a selector fails during `@Before`, record the exact Gradle/JUnit count. The 2026-08-29 attempt is historical. Current generic and Tournament live results are recorded in Progress. The `--print-database-url` helper still has a Compose naming limitation, so future reruns must use the active guarded database target until that helper is repaired.

The cleanup-contract command currently passes its behavior and build/dead-code checks. Rerun `bash scripts/tests/mobile-cleanup-contract.sh` from `apps/mobile` after future implementation changes and record its observed output. The scheduled `PROPOSED` acceptance and placement gate is complete for the tested Tournament fixture. Future reruns need the provider-disabled readiness check, the active guarded database target, the exact scheduled filter, and cleanup verification. The `--print-database-url` helper still has a Compose naming limitation.

## Outcomes & Retrospective

- Undated historical baseline (not current Issue #34 evidence): `apps/site`: `npx tsc --noEmit` passed after the final changes.
- Undated historical baseline (not current Issue #34 evidence; exact focused-test command not recorded): `apps/site`: focused Event Editor and operation replay tests passed: 33 tests.
- Undated historical baseline (not current Issue #34 evidence; exact focused-test command not recorded): `apps/site`: proposal route and display tests passed: 25 tests.
- (2026-08-25 15:42Z) Historical baseline (not current Issue #34 evidence): From `apps/site`, `npm run test:ci` passed with 874 suites passed, 5,216 tests passed, 4 skipped tests, and API route coverage for 330 files. This is a dated baseline, not a current expected count.
- Undated historical baseline (not current Issue #34 evidence; exact full-suite command not recorded): `apps/mobile`: full Android unit suite passed with `BUILD SUCCESSFUL`.
- Undated historical baseline (not current Issue #34 evidence; exact full-suite command not recorded): `apps/mobile`: full iOS simulator suite passed with `BUILD SUCCESSFUL`.
- Undated historical baseline (not current Issue #34 evidence; exact live-run command not recorded): `apps/mobile`: the generic One-Time Event mobile-to-site live run passed with the isolated seeded backend and database.
- Undated historical baseline (not current Issue #34 evidence): The first full site invocation used extra Jest arguments. The test suite passed, but the appended arguments reached route coverage and caused a false coverage-parser failure. The final unparameterized `npm run test:ci` passed.
- Accepted proposal replay now checks the submitted draft revision before returning its canonical result.
- Stale `PROCESSING` operation claims now use a conditional lease reclaim so an interrupted same-request retry can continue without allowing a different request to reuse the receipt.
- (2026-08-29 19:46:42.900Z) Historical dated run (not current post-refactor status): `apps/mobile` iOS `EventEditorTournamentParityCommonTest.given_shared_tournament_draft_when_command_is_encoded_then_complete_canonical_wire_is_preserved` passed with `tests=1`, `skipped=0`, `failures=0`, and `errors=0`.
- (2026-08-30 01:45:27.894Z) Historical dated run (not current post-refactor status): `apps/mobile` Android `EventEditorTournamentParityAndroidTest.given_shared_tournament_fixture_when_mobile_command_is_built_then_it_matches_the_web_golden` passed with `tests=1`, `skipped=0`, `failures=0`, and `errors=0`.
- Historical site batch (2026-08-30; not current post-refactor evidence): From `apps/site`, `npm test -- --runInBand src/server/events/__tests__/eventEditorSave.test.ts src/server/events/__tests__/eventEditorSave.database.integration.test.ts src/server/scheduler/__tests__/serialize.test.ts src/server/scheduler/__tests__/tournamentBracketMatrix.test.ts src/server/scheduler/__tests__/tournamentEditorParity.test.ts` reported 4 passed suites plus 1 skipped suite, 45 passed tests, and 5 skipped tests (50 total). The skipped suite contains database integration cases, so those cases were unexecuted.
- Historical pre-refactor complexity evidence (undated; not current status): From `apps/site`, `npm run lint:complexity -- src/server/scheduler/EventBuilder.ts src/server/scheduler/serialize.ts` exited 1 with 11 `EventBuilder.ts` complexity errors.
- Undated historical repository class-filter evidence (not a one-method result): From `apps/mobile`, `./gradlew --no-daemon :core:repository-impl:testDebugUnitTest --tests 'com.razumly.mvp.core.data.repositories.EventEditorTournamentParityAndroidTest'` reported 7 passed repository tests. The exact Android test case above is a separate one-test historical run.
- Undated local Compose class-filter evidence (not a Tournament live run): From `apps/mobile`, `./gradlew --no-daemon :composeApp:testDebugUnitTest --tests 'com.razumly.mvp.eventDetail.EventLifecycleMobileApiIntegrationTest'` reported 156 passed tests for that class. This class filter does not execute the standalone Tournament test case and is not backend evidence.
- Current-tree verification (2026-08-29): From `apps/site`, `npm run lint:complexity -- src/server/scheduler/EventBuilder.ts` exited 0 with no ESLint output, errors, or warnings after the EventBuilder refactor.
- Current-tree verification (2026-08-29): From `apps/site`, `npm test -- --runInBand src/server/scheduler/__tests__/tournamentBracketMatrix.test.ts` reported 1 suite passed, 7 tests passed, 0 skipped tests, 0 failures, 0 errors, and 0 snapshots.
- Historical class-filter evidence (2026-08-30; no current XML artifact): From `apps/mobile`, `./gradlew --no-daemon :core:repository-impl:testDebugUnitTest --tests 'com.razumly.mvp.core.data.repositories.EventEditorTournamentParityCommonTest'` reported 4 tests passed, `skipped=0`, `failures=0`, and `errors=0`. The exact current iOS one-method XML is recorded separately.
- Historical pre-fix Room XML evidence (2026-08-30T07:28:29.010Z): From `apps/mobile`, `./gradlew --no-daemon :core:repository-impl:testDebugUnitTest --tests 'com.razumly.mvp.core.data.repositories.EventRepositoryRoomPersistenceTest'` reported 7 tests passed, `skipped=0`, `failures=0`, and `errors=0`. The current 9-test Room result is reported generated evidence; its XML file is unavailable in the current worktree.
- Current-tree verification recorded 2026-08-30 (execution timestamp not supplied): From `apps/site`, `npm test -- --runInBand src/server/scheduler/__tests__/tournamentEditorParity.test.ts` reported one suite and 4 tests passed, with `skipped=0`, `failures=0`, and `errors=0`. The scheduled test case `matches the scheduled Tournament command and exact Match Demand` uses `test-fixtures/event-editor/tournament-parity-scheduled-web-golden.json`; this is local parity evidence, not a live backend result.
- Current-tree scheduled parity verification recorded 2026-08-30 (execution timestamp not supplied): the Android two-method selector for the unscheduled and scheduled fixture cases reported 2 tests with `skipped=0`, `failures=0`, and `errors=0`; the scheduled and unscheduled cases each assert Match Demand total 38, and the scheduled case asserts `FIXED_END` with `CREATE_AND_BUILD_SCHEDULE`. This is local evidence only.
- Historical failed readiness attempt (2026-08-29): The standalone Tournament selector used JDK 17 and the required environment, failed in `@Before` at `EventLifecycleMobileApiIntegrationTest.kt:782` after the isolation probe reached loopback port `3010` and received HTTP 500 from `/api/app-version`, and Gradle/JUnit reported `1 test completed, 1 failed`, `skipped=0`, `failures=1`, and `errors=0`. No create IDs existed, so cleanup was a no-op. The `2026-08-30T17:42:56.761Z` unscheduled XML pass is historical. The current `2026-08-30T19:03:28.014Z` result is reported generated XML evidence with its file unavailable in the current worktree, records one test with zero skipped/failures/errors, and is one unscheduled `CREATE_ONLY` saved graph only; cleanup and residual checks passed. The current scheduled backend-backed result is recorded separately below.
- Current cleanup-contract verification: From `apps/mobile`, the Node behavior check and `bash scripts/tests/mobile-cleanup-contract.sh` emit `Mobile cleanup behavioral contract passed` and `Mobile build/dead-code cleanup contract passed`. The checks cover loopback/non-production guards, provider-disabled execution, receipt settling, transaction rollback/commit, exact operation-and-Event deletion, and residual checks.
- Final post-fix two-axis review (2026-08-30; execution timestamp not supplied) remains clean for Room graph, cleanup, and Plans Standards and Specification. The three cleanup findings—generic teardown durable-receipt ownership, dispatched generic-receipt settlement, and in-flight Tournament purge—each moved `OPEN` -> `FIXED` -> `RE-REVIEWED` -> `VERIFIED`; the cleanup helper is staged and all plan findings are verified. The current backend-backed scheduled run below supplies the previously pending `PROPOSED` acceptance and placement evidence; this does not claim a new review cycle.
- Current iOS parity XML observed at `2026-08-31T02:53:16.515Z` reports 4 tests with zero skipped, failures, or errors, including the scheduled wire case.
- Current Room XML observed at `2026-08-31T03:00:56.757Z` reports 10 tests with zero skipped, failures, or errors. It covers graph identity replacement, accepted graph persistence, narrow GET graph preservation, and malformed response guards.
- Current site persistence evidence: `npm test -- --runInBand src/server/repositories/__tests__/events.upsert.test.ts` reports 1 suite and 92 tests passed. `npx tsc --noEmit` exits 0.
- Current backend-backed Tournament XML observed at `2026-08-31T03:00:29.165Z` reports `tests=2`, `skipped=0`, `failures=0`, and `errors=0` for exact class selector `--tests 'com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest'`. Later Compose filters replaced the shared report directory. Both unscheduled and scheduled methods passed. The scheduled method verifies `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END`, automated/support flags, backend `PROPOSED` acceptance, one operation-bound acceptance PUT, placed Match Graph, persistence/reload, Room graph/resources, cleanup, and residual checks.
- Current cleanup source/fixture evidence (2026-08-31): `purge-mobile-tournament-test-run.mjs` requires terminal create dispatch, no acceptance dispatch, a server-shaped receipt event ID, null response/proposal JSON, `proposalStatus=NONE`, and `emailDelivery=PROCESSING` for a terminal failed-create receipt. `runTypedCreateRejectionPurge` supplies the empty resolved event ID, terminal create flag, cleared graph tables, and null/`NONE`/`PROCESSING` receipt fields, then verifies commit and receipt deletion.
- Current source trace: `EventRepository.saveEventEditor` and `EventCacheMerge` preserve authoritative graph identities and team relations for `BUILT`/`REBUILT` responses; `PRESERVE` responses use `EventEditorSaveResultDto.matchesForPersistence`, while graph-bearing create/accept and `BUILT`/`REBUILT`/reconcile responses use `EventEditorCreateProposalGraphDto.decodeOrThrow`. The two malformed Room methods use `NOT_REQUESTED`/`PRESERVE`, the valid rebuilt method uses `REBUILT`, and the valid accepted method uses create `PROPOSED` then accept `BUILT`. Current Room XML is directly inspectable and reports 10 passing tests.
- Current generic live evidence supersedes the historical infrastructure blocker: the exact One-Time Event selector reports one test with zero skipped, failures, or errors, and the persisted reload assertions pass. The earlier Compose URL derivation failure remains a reproducibility limitation for the helper, not a product failure.

## Revision Note

2026-08-24: Created for issue #37. The plan records the rollback-only proposal computation, durable operation receipt, exact accept path, authoritative revision binding, and site/mobile review flows.

2026-08-25: Added stale accepted-replay validation and conditional abandoned operation-claim recovery after the two-axis review. Recorded final site, mobile, and mobile-to-site verification.
Historical revision record (2026-08-30 01:46Z): At that earlier revision the Tournament live test had not yet been executed, so the plan recorded it as unrun. The current review-cycle record supersedes that status with the JDK 17 selector's failed `@Before` result on port 3010, one completed/failed test, and cleanup no-op; the gate remains blocked and no pass is claimed.
Historical revision record (2026-08-29): At that earlier revision the readiness blocker and no-runtime-repair wording were intentionally unchanged. The current review-cycle evidence above records the failed selector counts, no create IDs, and cleanup no-op.
2026-08-30: Corrected the Issue #34 source boundary, Room and cleanup evidence, current-tree status, and review status while preserving the schedule-proposal history.
Historical revision record (2026-08-30): Relabeled undated historical test evidence and the dated parity runs as historical post-refactor status so current-tree counts and the blocked Tournament live run cannot be conflated.
Historical revision record (2026-08-29): Revised `## Purpose / Big Picture` (lines 5-11), `## Progress` (12-34), `## Milestones` (35-43), `## Surprises & Discoveries` (44-57), `## Plan of Work` (119-131), `## Concrete Steps` (132-204), `## Validation and Acceptance` (205-219), `## Idempotence and Recovery` (220-226), and `## Outcomes & Retrospective` (227-251). The revision records the exact JDK 17 `@Before` failure at `EventLifecycleMobileApiIntegrationTest.kt:782`, HTTP 500 on loopback port 3010, `1 test completed, 1 failed`, zero skips/errors, and cleanup no-op; updates current parity to 3 tests and Room to 5 tests; records the cleanup-contract blocker; and replaces broad M1 control guidance with named existing test cases and explicit coverage limits.
Historical revision record (2026-08-29): Historical review-cycle note retained for provenance; its prior cleanup-contract wording is superseded by the exact then-current `-Xmx4g...` requirement versus current `gradle.properties` `-Xmx8g...` blocker recorded in this plan. The Tournament live gate remains blocked.
Historical revision record (2026-08-29): Final review-cycle revision after the failed Tournament live attempt. Updated `## Purpose / Big Picture` (lines 5-12), `## Progress` (13-36), `## Milestones` (37-46), `## Surprises & Discoveries` (47-61), `## Context and Orientation` (83-90), `## Context Boundary` (91-122), `## Plan of Work` (123-136), `## Concrete Steps` (137-242), `## Validation and Acceptance` (243-258), `## Idempotence and Recovery` (259-266), and `## Outcomes & Retrospective` (267-292). Recorded the exact JDK 17 `@Before` HTTP 500 readiness failure on loopback port 3010, `1 test completed, 1 failed`, `skipped=0`, `failures=1`, `errors=0`, no create IDs, cleanup no-op, current 3-test parity and 5-test Room class-filter results, the cleanup-contract `-Xmx4g...` versus `-Xmx8g...` blocker, and criterion-specific immediate-edit evidence with explicit coverage limits. The live gate remains blocked.

Historical revision record (2026-08-30): Corrected the stale cleanup-contract artifact claim in `## Milestones` (37-46), retained the exact then-current `-Xmx4g...` versus `-Xmx8g...` blocker, and preserved the blocked Tournament live evidence. Final ranges: `## Milestones` (37-46), `## Surprises & Discoveries` (47-61), `## Outcomes & Retrospective` (267-292), and `## Revision Note` (293-306).
Historical revision record (2026-08-30): Final review-cycle correction after the scheduled-parity evidence update. Updated the current evidence references in `## Progress` (13-38), `## Milestones` (39-48), `## Surprises & Discoveries` (49-63), `## Plan of Work` (126-139), `## Concrete Steps` (140-257), `## Validation and Acceptance` (258-273), `## Idempotence and Recovery` (274-281), and `## Outcomes & Retrospective` (282-308). At that historical revision, current common parity was 4 tests; site parity was one suite and 4 tests; the Android scheduled+unscheduled selector was 2 tests; and Room persistence was 5 tests. Older revision entries that mention 3-test parity are historical records. The scheduled sibling retained `FIXED_END`, `CREATE_AND_BUILD_SCHEDULE`, and Match Demand total 38 evidence. The current cleanup-contract run emitted `Mobile cleanup behavioral contract passed` and `Mobile build/dead-code cleanup contract passed` after aligned receipt assertions and stale `RefundRequestDTO` removal; the JDK 17 `@Before` HTTP 500/no-create-ID cleanup no-op remained and the Tournament live gate remained blocked. Final range: `## Revision Note` (309-323).
Historical revision record (2026-08-30): Updated all current cleanup-contract references after the aligned receipt assertions and stale `RefundRequestDTO` removal. The exact script output is `Mobile cleanup behavioral contract passed` and `Mobile build/dead-code cleanup contract passed`; the earlier `-Xmx4g...` versus `-Xmx8g...` wording remains historical only. Updated `## Progress` (13-38), `## Milestones` (39-48), `## Surprises & Discoveries` (49-63), `## Plan of Work` (126-139), `## Concrete Steps` (140-257), `## Validation and Acceptance` (258-273), `## Idempotence and Recovery` (274-281), `## Outcomes & Retrospective` (282-308), and `## Revision Note` (309-324); the Tournament JDK 17 readiness failure and blocked live gate remain unchanged.
Historical revision record (2026-08-30): Corrected the remaining active `## Plan of Work` cleanup sentence (line 138) to the observed current pass. The `-Xmx4g...` versus `-Xmx8g...` wording remains only in historical revision records. Re-read `## Plan of Work` (126-139), `## Concrete Steps` (140-257), `## Validation and Acceptance` (258-273), `## Idempotence and Recovery` (274-281), `## Outcomes & Retrospective` (282-308), and `## Revision Note` (309-325); at that historical revision, current common parity was 4 tests, Room was 5 tests, and the JDK 17 Tournament readiness failure kept the live gate blocked.
Historical revision record (2026-08-30): Recorded the completed two-axis standards/spec review at fixed base `bd683fb` with no actionable findings. Updated current review status in `## Progress` (13-38); preserved the unrerun iOS selector and blocked Tournament live gate. Final revision range: `## Revision Note` (309-326).
Historical revision record (2026-08-30T07:28:29.010Z): Updated all active Room persistence references to the observed `EventRepositoryRoomPersistenceTest` XML result (`tests=7`, `skipped=0`, `failures=0`, `errors=0`). Preserved older 5-test and `-Xmx4g...` versus `-Xmx8g...` entries as historical revision records, and preserved the blocked Tournament live gate. Final ranges: `## Progress` (12-38), `## Milestones` (39-48), `## Surprises & Discoveries` (49-63), `## Plan of Work` (126-139), `## Concrete Steps` (140-257), `## Validation and Acceptance` (258-273), `## Outcomes & Retrospective` (282-308), and `## Revision Note` (309-327).
Historical revision record (2026-08-30): Final post-review consistency correction. Updated the missed active Room statement in `## Concrete Steps` (line 256) to the then-current 7-test XML result and marked the earlier 5-test revision entries in `## Revision Note` (lines 323 and 325) as historical. The then-current common parity remained 4 tests, the cleanup-contract checks remained a recorded pass, and the Tournament live gate remained blocked by the exact JDK 17 `@Before` HTTP 500/no-create-ID attempt. Final ranges: `## Concrete Steps` (140-257) and `## Revision Note` (309-328).

2026-08-30: Reconciled the post-fix living evidence. The 2026-08-30T17:42:56.761Z unscheduled Tournament XML and the 2026-08-30T07:28:29.010Z seven-test Room XML are historical. The current records are the iOS one-test XML at 2026-08-30T18:34:08.735Z, the nine-test Room XML at 2026-08-30T19:01:26.799Z, the unscheduled `CREATE_ONLY` Tournament XML at 2026-08-30T19:03:28.014Z, and the focused site playoff omission/explicit-`null` result of 3 passed, 89 skipped, 0 failures, and 0 errors. Replaced the obsolete HTTP test reference with the existing `EventEditorRemoteGatewayTest.kt` path and exact MockEngine filters, corrected the retry matrix paths, recorded graph identity/team and playoff traces, and left post-fix review plus scheduled `PROPOSED` acceptance and placement pending.

2026-08-30: Added the all-mode `EventEditorSaveResultDto.matchesForPersistence` identity trace and the two current Room cache-unchanged guard methods; recorded the generic receipt cleanup helper's exact delete, OR residual check, transaction and local guards, Node synthetic evidence, and the current generic-selector `@Before` blocker with no receipt/body. Updated the readiness command to request `mvpTestIsolation=1` and verify provider-disabled state plus the matching guarded database URL hash, added provider-disabled environment to the generic selector, and made dynamic route Jest suites use `--runTestsByPath`. The current unscheduled `CREATE_ONLY` result remains the only live claim; scheduled `PROPOSED` acceptance/placement and post-fix review remain pending.
2026-08-30: Corrected the live-scope wording so the Tournament selector claims only one unscheduled `CREATE_ONLY` saved graph and attributes all-mode identity to local Room tests; mapped `PRESERVE` to `EventEditorSaveResultDto.matchesForPersistence` and graph-bearing create/accept/`BUILT`/`REBUILT`/reconcile responses to `EventEditorCreateProposalGraphDto.decodeOrThrow`, with explicit `NOT_REQUESTED`/`PRESERVE`, `REBUILT`, and create-`PROPOSED`/accept-`BUILT` test modes. Added the three cleanup source paths and the readiness blocker, marked Room and Tournament XML as reported generated evidence unavailable in the current worktree, kept the iOS XML directly inspectable, and retained historical evidence, pending scheduled `PROPOSED` acceptance, and pending post-fix review.
- 2026-08-30: Recorded the final post-fix two-axis review as clean for Room graph, cleanup, and Plans Standards and Specification. The three cleanup findings—generic teardown durable-receipt ownership, dispatched generic-receipt settlement, and in-flight Tournament purge—each moved `OPEN` -> `FIXED` -> `RE-REVIEWED` -> `VERIFIED`; `purge-mobile-event-editor-receipts.mjs` is staged and all plan findings are verified. Recorded the separate generic-selector infrastructure blocker: current generated XML is unavailable; reported `tests=1`, `skipped=0`, `failures=1`, and `errors=0` in `@Before` because readiness JSON omitted `outboundProvidersDisabled` and `databaseUrlHash`; no Event Editor request or receipt was created. No generic live pass is claimed. Scheduled `PROPOSED` acceptance and placement remains pending.
- 2026-08-31T00:08:16.201Z: Added the current backend-backed scheduled Tournament class result. The XML `apps/mobile/composeApp/build/test-results/testDebugUnitTest/TEST-com.razumly.mvp.eventDetail.MobileTournamentEventEditorApiContractTest.xml` reports `tests=2`, `skipped=0`, `failures=0`, and `errors=0`; both unscheduled and scheduled methods passed after private hash preflight against healthy `issue34-site-db` on loopback `5543`. The scheduled method proves `CREATE_AND_BUILD_SCHEDULE`, `FIXED_END`, automated/support flags, backend `PROPOSED` acceptance, placed Match Graph, persistence/reload, Room graph/resources, cleanup, and residual checks. Four daily windows and `gamesPerOpponent=1` supplied valid capacity while retaining pool/bracket demand. Recorded the terminal failed-create receipt predicate and `runTypedCreateRejectionPurge` synthetic fixture fix. The prior `mvp-site-db:5433` hash mismatch remains historical ownership-conflict evidence; the current worktree's `--print-database-url` helper is not claimed to work. The earlier final review lifecycle and the generic-selector infrastructure blocker remain recorded without a new review claim.
- 2026-08-31T03:00:29.165Z: Updated the live schedule-proposal evidence with the latest Tournament class XML. It reports two passing tests with zero skipped, failures, or errors. The scheduled case verifies the complete tested `PROPOSED` acceptance path, operation-bound acceptance dispatch, placements, Room-first graph persistence, reload, and run-owned cleanup. Added current Room, generic, Android/iOS parity, setup, site scheduler, site persistence, TypeScript, and cleanup evidence to Progress.
- 2026-08-31T03:00:29.165Z–03:07:52.434Z: Re-ran the final direct checks after the scheduled test assertion fixes. The Tournament live class reports 2 tests passed with zero skipped, failures, or errors. The Room class reports 10 tests passed with zero skipped, failures, or errors. Android parity reports 2 tests passed; iOS parity reports 4 tests passed. The three named setup tests pass, and the retry/failure selector reports 4 tests passed. Site scheduler, bracket, and parity checks report 3 suites and 22 tests passed; site retry/save checks report 2 suites and 40 tests passed; the full repository upsert file reports 1 suite and 92 tests passed; TypeScript and complexity lint exit 0. Cleanup behavior and contract checks pass.