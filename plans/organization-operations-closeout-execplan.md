# Complete the Organization operations quality checks

This ExecPlan follows `PLANS.md`. Keep the progress, decisions, and results current.

## Purpose / Big Picture

Finish issue 123 without changing the reviewed Organization layouts or their behavior. The user must retain working filters, data-only loading placeholders, calendar interactions, permissions, and financial actions. The code must also pass the repository's complete changed-file complexity check before it is committed.

## Progress

- [x] (2026-09-07) Fix Select dismissal, the Today minimum-date shortcut, rental search consistency, and failed rental refresh reporting. Replace the remaining FieldCalendarFilter and TeamCard Mantine imports with owned controls. Add separate calendar lanes for simultaneous entries. Add keyboard creation actions for rental, staff, and official drafts. Preserve the rental timeZone in the calendar save draft. The final combined run passes all 140 tests across nine suites. Report: `C:/Users/samue/AppData/Local/Temp/issue123-focused-final.json`.
- [x] (2026-09-07) Build current source and start the approved production preview on port 3155. Final build `6MDcdlDWqjSFzL2WPI-5K` passes TypeScript and generates 127 static pages. The process is 38244. `/api/sports` returns HTTP 200. The final focused lint check reports zero errors and three advisory warnings. Temporary build config additions were removed after startup. No database or environment file changed.
- [x] (2026-09-07) Verify the final source through Codex browser keyboard controls. Resources opens with Enter. All selects five resources. Edit mode retains controls during data loading. Add rental slot enables Save and Undo. Undo disables Save. Discard exits edit mode. Escape restores the saved Facility label after typing. Switching to Customers retains headings, search, filters, and table headers while rows load. Desktop and 390-pixel mobile inspection confirms separate overlapping entries and no All-button overlap. Mouse drag checks remain open because click and pointer input did not respond through the available automation paths.
- [ ] (2026-09-07) Complete the remaining pointer-interaction checks and separate Standards/Spec reviews. No sub-agent tool is available. The user was asked to authorize two read-only review tasks. Do not close the issue or stage the mixed worktree as one commit before the final scope review.

- [ ] (2026-09-06) Resolve the current review findings after #155 verification. Standards confirms silent rental-load failures, different cached/refreshed rental search predicates, and the Today minimum-date bypass. Spec confirms overlapping resource entries, retained uncommitted Select text, pointer-only manager creation, and remaining Mantine imports in FieldCalendarFilter and Customer detail TeamCard. The rental modal migration is now complete under #155. Do not treat the earlier review findings as resolved. The Store and Discounts shared-pattern approval remains valid.

- [x] (2026-09-05) Pass the production build with a process-only 6 GB heap limit and no concurrent Jest run. TypeScript passed. Next.js generated 127 static pages. Start the approved local preview on port 3000 in production mode. No database or environment file changed.
- [x] (2026-09-05) Restore isolated preview data with explicit user approval. Create `bracketiq_e2e_123_c2a7`. Apply all 209 worktree migrations and confirm current status before seeding. Copy 13 events, 16 teams, and their test-user and team-assignment data from a read-only snapshot of the local issue-151 database. Add missing Organization preview fixtures. Change only this worktree's database connection. Restart the preview in production mode. Verify the host login and populated Events, Teams, and Facilities routes in the Codex browser. The missing-database error is resolved. See `plans/organization-operations-closeout-verification.md` for evidence and remaining interaction checks.
- [x] (2026-09-05) Complete the broad behavior run and isolated retry. Across both runs, 469 distinct tests pass in 71 suites. All 26 Finance tests pass on retry with the existing timeout and no source changes. Create Team and its logo workflow also pass. The unchanged Node hydration suite cannot start because of the existing shared `MouseEvent` setup error. Reports are in `apps/site/node_modules/.cache/issue-123-final-behavior-results.json` and `issue-123-final-isolated-results.json`.
- [x] (2026-09-05) Resolve the Store and Discounts dedicated-reference decision. The user explicitly approved review against the existing shared design patterns. Dedicated images are not required for these two tabs. Their behavior, accessibility, and visual checks remain required. This decision supersedes the earlier pending-reference notes in this plan.
- [ ] (2026-09-05) Finish the final behavior and visual audit. The user approved a new local production build and start or restart. The 70-file behavior run passed 68 suites and failed two suites. It passed 458 tests; three Finance tests timed out. The unchanged `fieldCalendarHydration.test.ts` stopped at the existing Node `MouseEvent` setup failure. Rerun Finance separately after the build. The production code compiled after a network-enabled font-fetch retry. Build type checking and current-build browser checks remain in progress.
- [ ] (2026-09-05) Resolve the final independent review findings before closure. Standards review found silent rental-load failures and different search predicates for cached and refreshed rental events. Review also found a minimum-date bypass through Today. Spec review found overlapping calendar entries, a Select display/value mismatch after dismissal, pointer-only calendar creation, and nested Mantine imports in `CreateRentalSlotModal`, `FieldCalendarFilter`, and Customer detail's `TeamCard`. Confirm each finding through behavior checks. Keep the existing missing Store and Discounts reference approvals open.
- [x] (2026-09-04) Read issue 123, its comments, repository rules, coding standards, and review instructions.
- [x] (2026-09-04) Record the current branch and failing quality check.
- [x] (2026-09-04) Refactor the shared controls, Organization header, Overview details, Store card interactions, and resource-grid helpers. Their focused complexity checks pass.
- [x] (2026-09-04) Add a shared Organization loading hook. Keep same-Organization controls mounted after saves and creates. Report silent refresh failures and reject stale responses.
- [x] (2026-09-04) Fix event query debounce, cache scope, rental classification, and pagination races. Fix calendar time conversion, resource drops, viewport bounds, and short-entry movement.
- [x] (2026-09-04) Extract customer response mapping, Organization access decisions, customer bill and document views, staff roster presentation, calendar classification, and calendar range edits. Each new module passes its focused complexity check.
- [x] (2026-09-04) Verify the controller-extraction slice. All 76 tests in 10 affected suites pass. TypeScript and `git diff --check` pass.
- [x] (2026-09-04) Move Organization event requests and cache state into focused modules. Keep filter state in the page. Simplify the Reviews summary, notices, editor, and confirmation views. The new event modules and the complete Reviews panel pass the complexity check.
- [x] (2026-09-04) Verify the event-request and Reviews slice. All 46 tests in six suites pass. TypeScript and `git diff --check` pass. The complete changed-file check reports 102 errors and 18 warnings.
- [x] (2026-09-04) Separate Facilities staff-edit preparation, occurrence selection, and resource context from the calendar UI. Keep the existing undo queue and HTTP calls. Each new module and each staff-submit workflow passes complexity.
- [x] (2026-09-04) Verify the Facilities staff-edit slice. All 72 tests in four suites pass. TypeScript and `git diff --check` pass. The complete changed-file check reports 97 errors and 18 warnings.
- [x] (2026-09-05) Migrate BillingAddressModal and BillingAddressFields to the owned controls. Keep fields visible during loading. Add load retry and disable edits and dismissal during saves. All five dialog tests, TypeScript, and `git diff --check` pass. The complete changed-file gate reports 97 errors and 18 warnings across 83 files, with no findings in the billing slice.
- [x] (2026-09-05) Migrate CreateTeamModal, ImageUploader, and ImageSelectionModal to owned controls. Separate team draft validation, form sections, and document-template loading. Keep editable fields and drafts during template loading and retry. Block duplicate creation, edits, and dismissal during pending saves. Make saved-image selection keyboard accessible. All 25 tests in five affected suites pass, including the nested team-logo workflow.
- [x] (2026-09-05) Verify the complete Create Team and image-picker slice. TypeScript and `git diff --check` pass. All changed files in this slice pass complexity. The complete workstream gate still reports 97 errors and 18 warnings in other files. No commit, runtime restart, database change, or deployment occurred.
- [x] (2026-09-05) Migrate the payment and rental dialogs used by Organization Store and Facilities. Separate payment dialog state, Stripe Elements composition, result presentation, and rental dialog presentation. Keep payment requests, displayed amount calculations, reservation writes, and lock handling unchanged. The seven affected dialog suites pass all 34 tests.
- [x] (2026-09-05) Verify the checkout source slice. TypeScript and `git diff --check` pass. Its complete changed files pass complexity. The workstream gate still reports 97 errors and 18 warnings elsewhere. No runtime, database, environment, or deployment changed.
- [x] (2026-09-05) Separate Discounts pricing and result views. Separate Refunds reference loading and requester/host table presentation. Keep price calculations, code actions, approval previews, and decision requests unchanged. Report failed or incomplete refund name reads before showing actionable requests.
- [x] (2026-09-05) Verify the Discounts and Refunds slice. All 19 tests in five affected suites pass. TypeScript and `git diff --check` pass. The complete files in this slice pass complexity. The workstream gate now reports 93 errors and 18 warnings. No runtime, database, environment, build, or deployment changed.
- [x] (2026-09-05) Separate Divisions draft preparation, display names, the table, and the editor. Keep price conversion and existing write requests. Show save errors inside the editor. Block input changes and dismissal during a pending save. Resolve sport, gender, age, and skill labels from their catalogs.
- [x] (2026-09-05) Separate Public Page draft state, slug readiness, event search, and widget snippet generation. Keep the reviewed layout and existing save payload. Use one widget-option mapping for URL parameters and script attributes. The complete source files pass complexity. The workstream gate now reports 84 errors and 18 warnings.
- [x] (2026-09-05) Verify the Divisions and Public Page slice. The final combined run passed all 23 tests in six suites. TypeScript, focused complete-file lint, and `git diff --check` pass. The workstream gate still reports 84 errors and 18 warnings elsewhere. No runtime, database, environment, build, or deployment changed.
- [x] (2026-09-05) Separate Finance payroll filters, ledger totals, CSV output, profitability grouping, and line-item draft preparation. Preserve the current financial calculations, export columns, save payloads, and reviewed layout. The first five-suite run passed 50 tests. The changed-file gate fell from 84 errors to 79 errors, with 18 warnings.
- [x] (2026-09-05) Verify the Finance data-preparation slice. The final combined run passed all 52 tests in five suites. TypeScript, focused lint for the new data modules and affected tests, and `git diff --check` pass. The complete changed-file gate reports 79 errors and 18 warnings. Seven errors and one warning remain in the Finance panel. No runtime, database, environment, build, or deployment changed.
- [x] Separate the remaining Finance display sections and finish its complete-file complexity checks. Keep data-only loading, keyboard navigation, payroll actions, accounting status, and dialogs stable. All 53 tests in five affected suites pass. TypeScript and focused complete-file lint pass. The issue-wide gate now reports 72 errors and 17 warnings.
- [ ] Review remaining UI complexity warnings for unclear responsibilities or behavior risks. The user-approved policy makes JSX and TSX complexity advisory. A warning alone does not require another extraction.
- [ ] Verify the Organization tab and dialog display extraction started before the policy change. The named render functions are in place and TypeScript passes. Its affected behavior checks remain pending. Further extraction must improve a clear responsibility rather than only reduce a complexity score.
- [x] (2026-09-05) Share Facilities interval and recurrence preparation. Preserve rental availability, staff coverage gaps, repeat dates, resize rules, and draft state. All 84 tests in four affected suites pass.
- [x] (2026-09-05) Separate Facilities display sections, defaults, editor setup, and shared selection-range preparation. Remove all 14 missing callback dependency warnings. The draft workflow proves retained input focus, saved notes on reopen, and restored notes after undo. TypeScript and `git diff --check` pass. The changed-file gate reports 47 errors and one image warning.
- [x] (2026-09-05) Share calendar move and resize dispatch. Reuse the card/grid permission decision. Share drag-handle detection and staff editor defaults. Preserve separate open-shift and assigned-shift actions. Remove an unreachable manager summary from the public-only summary path. All 110 tests in six affected suites pass. TypeScript and `git diff --check` pass.
- [x] (2026-09-05) Finish Facilities hydration, selection updates, and save coordination. The complete Facilities controller passes strict lint. The full changed-file gate reports 30 errors and one image warning across 137 files. The errors are in the Organization page (17) and Discover (13). Preserve queued writes and the existing HTTP contracts.
- [x] (2026-09-05) Separate Facilities save execution from UI state. Preserve draft ordering, parent-child links, rental updates, staff override ordering, and failure propagation. All 11 save-interface tests pass. The recovered integrated run passed 55 tests. The final combined Facilities run passed 130 tests in nine suites. Failed saves retain draft and undo state. Pending saves block duplicate requests, Undo, and Discard. Rental drag rejection now occurs before a React state update.
- [x] (2026-09-05) Separate staff calendar move and resize preparation. Preserve child containment, repeat dates, resource changes, child clipping, and grouped undo. The direct range run passed 34 tests in three suites. The integrated run passed 96 tests in five suites.
- [x] (2026-09-05) Separate rental checkout bounds, resource context, callback values, and URL parameters. Share expanded-facility lookup, target-resource lookup, and date parsing. Keep rental range rules separate from staff range rules. The final combined run passed 109 tests in seven suites. TypeScript and `git diff --check` pass. Five Facilities controller errors remain in the focused check.
- [x] (2026-09-05) Remove the unreachable Staff calendar and finish the active roster paths. Preserve Facilities scheduling, roster filters, invitations, role updates, and compensation. A failed role creation now requires explicit retry. The Staff and calendar-card rerun passed 22 tests in three suites. TypeScript and focused lint pass.
- [x] (2026-09-05) Separate Facility details validation, selected-resource data, save preparation, and editor sections. Preserve equivalent-source draft retention. All 50 tests in two affected suites pass. TypeScript, focused complete-file lint, and `git diff --check` pass.
- [x] (2026-09-05) Separate Discover event defaults, active filter chips, filter sections, and result states. Keep its public props, cache behavior, sorting, and layout unchanged. Replace static checks with filter, sorting, loading, click, and failed-refresh workflows. Simplify three Facilities test paths without removing assertions. All 68 tests in four suites pass. TypeScript passes. The changed-file gate reports 56 errors and 15 warnings.
- [x] (2026-09-04) Run the final affected behavior tests and TypeScript. All 140 tests in 15 suites pass. TypeScript and `git diff --check` pass.
- [x] (2026-09-04) Complete separate Standards and Spec follow-up reviews of the named fixes. Neither follow-up has an open finding in that scope.
- [x] (2026-09-05) Apply the user-approved complexity policy. JSX and TSX warn above 20. Non-JSX functions fail above 10. Control-flow nesting remains limited to four levels. The complete changed-file gate passes with zero errors and eight warnings. All 12 policy tests and TypeScript pass.
- [ ] Complete the remaining issue-wide acceptance checks.
- [ ] Resolve reference-approval gaps and verify applicable responsive views.
- [ ] Commit verified work and complete the authorized issue workflow when its acceptance criteria pass.

## Surprises & Discoveries

The final save-retry failure came from dropping `timeZone` in `buildRentalSlotUpdateFromCalendarRange`. The repeating rental fixture uses America/Los_Angeles. Its saved result fell back to UTC after the draft lost that field. Preserving the existing timezone makes the unchanged end-to-end UI assertion pass. An attempted non-repeating timestamp change did not resolve the failure and was reverted. No request field became required, and no API route or mobile file changed in this continuation.

The interrupted Facilities validation completed successfully. Its recovered sessions reported 55 passing tests and a clean TypeScript check. Later changes were verified separately. A calendar conflict check used a flag mutated inside a React state updater. The new pre-update check has an interaction regression for retained selection and date.

The current branch is `workstream/site-ui-operations` at `4c9de6191`. It contains earlier issue 123 commits and uncommitted reference-alignment and loading changes. Preserve all of these edits. The initial changed-file check reported 131 complexity errors and 21 warnings across 47 site files. Later results are recorded below. Large controllers cause most errors. The rule applies to complete changed files and prohibits exceptions or suppressions.

Issue 123 is open and assigned to the current operator. Its surface checklist is complete, but comments still require approved Store references and final visual review. The reference-alignment plan also records no dedicated Discounts reference. Verify these gaps before closure. Do not invent an approval.

The local production preview is already running on port 3000. This cleanup request does not authorize a new restart. Keep it running. A later source build must use separate output or wait for a current restart instruction so it does not overwrite files used by the active preview.

## Decision Log

On 2026-09-07, rental refreshes became all-or-nothing at the local cache boundary. One failed field read now rejects the refresh. The controller keeps the previous cache and exposes its existing retry error. No backend request or save contract changes. Hosted and refreshed rental search now share `matchesEventSearch`. Calendar lanes separate simultaneous intervals without changing their time values. Native Add buttons create drafts through the existing queue and Undo path. The user approved rebuilding and restarting only port 3155.

The user approved a React-specific complexity policy on 2026-09-05. `apps/site/eslint.complexity.config.mjs` keeps the strict non-JSX limit of 10 and applies an advisory threshold of 20 to JSX and TSX. The override changes only complexity. Nesting and the other lint rules remain active. This is a file-based policy, not automatic recognition of React functions. Separate scheduling, payment, validation, and data-processing logic remains in non-JSX modules. Rendering warnings prompt a review of responsibilities; they do not justify extraction by themselves. Local, staged, and CI lint share the same configuration. `apps/site/CODING_STANDARDS.md` is the policy reference for agents.

The Organization display slice uses private render functions for named tabs and dialogs. These are ordinary function calls, not nested React component types. Existing controls keep their identity and state. Focused presentation functions will remove repeated verification and customer-detail branches. The page retains data requests and mutations. No provider interface, runtime, or database change is part of this slice.

The Facilities save module executes the existing requests and returns changed records and removed IDs. The controller retains permission checks, pending-save protection, state publication, notifications, queue clearing, and refreshes. This is not a transaction change. The recorded partial-save risk remains until the backend save contract changes. Tests use internal application services and do not simulate an external provider.

Rental drag validation now runs before the React state update. The old handler changed a local rejection flag inside a functional state updater and then read the flag immediately. React does not guarantee that update timing. The handler now validates the current selection before publishing the range or calendar date. It retains a functional update for accepted changes. Private manager and public selection functions keep their separate rules. Hydration still compares event and match IDs before replacing field data. This cleanup does not change the existing same-ID freshness rule.

The rental checkout slice prepares callback data and the schedule URL in `facilityRentalCheckout.ts`. The controller retains sign-in checks, rental readiness checks, selection serialization, ID creation, and the checkout callback. The helper keeps the original timestamp format, parameter order, price rounding, document IDs, host choice, and metadata fallback rules. An unresolved facility link remains in callback data but does not become a hydrated facility URL parameter. No payment request changed.

Resource moves and checkout now share expanded-facility lookup. Rental and staff drops share date parsing and target-resource lookup. Date parsing does not enforce duration or same-day constraints. Each workflow retains its own range rules. A blank event resource ID remains different from a missing ID, as in the original fallback logic. Rental modal updates prepare the same queued change before the controller stages it.

The next Facilities slice moves staff calendar range-change decisions into a pure module. It returns staged overrides or the existing validation error. The controller retains pointer suppression, the undo queue, notifications, and all persistence. Parent resize preparation preserves child-before-parent override order. No backend transaction, HTTP contract, or runtime operation is part of this slice.

The Facilities interaction slice reuses `canChangeFacilityEventRange` and `isStaffFeedEvent` for both the grid and controller. Move and resize dispatch through one handler. Resize retains the current resource; move retains its target-resource fallback. One selector resolver detects drag handles across nested targets. Staff editor preparation retains the different resource rules for open coverage and assigned coverage. The public summary is only rendered when the viewer cannot manage Facilities. Its manager branch and unused conflict calculation were unreachable and are removed. This does not remove the manager calendar conflict layer or change its counts.

The Facilities display slice uses private render functions and a stable top-level workspace. Optional props are normalized before the stateful workspace renders. The editor controls keep their React identity. Rental and staff editors share selection validation. Storage hydration and facility changes share the initial selection range. Callback dependencies include the stable queue setters and the full Organization used by rental checkout. No HTTP request, schedule payload, layout, or public prop changed.

The next Facilities slice uses the codebase-design skill to separate calendar interval and occurrence preparation from UI state. Public rental availability and staff coverage use the same interval subtraction rules. Staff occurrence lookup and feed expansion use one scheduled-day decision. Draft expansion keeps its rental and staff date rules. The controller retains draft queues, selection, and all HTTP operations. Tests must prove coverage gaps, recurrence bounds, resource identity, and resize behavior before the extracted functions replace the local paths.

The Discover event slice uses private render functions for filter sections and result states. A stable top-level view receives normalized optional props. This keeps React Compiler compatible with the existing memoized callbacks and filters. The default preparation remains outside the stateful view. These private functions do not create nested component types. Filter input elements keep their identity when cache data changes. No event request, analytics event, sorting rule, or page layout changed.

Facility details uses focused private functions for validation, saved-record preparation, selection, and editor sections. It reuses the Facilities resource-link resolver. The source-reset effect compares saved values before replacing drafts. Equivalent response objects do not clear edits or undo history. Existing facility and resource requests remain sequential. This refactor does not make the related save atomic. The existing partial-save risk still requires a backend transaction decision.

Calendar-card presentation and interaction permissions now use a small shared module. The grid uses one move/resize permission decision. Real card and grid tests cover draft actions, staff assignment activation, drag suppression, and keyboard resizing. No provider interface, runtime, database, or HTTP contract changed.

The next slice uses the codebase-design skill to remove an unused duplicate and simplify the live Staff paths. `RoleRosterManager.tsx` offers Staff, Roles, and Compensation views. Its final render never calls `renderScheduleView`. The old calendar state, occurrence builder, and schedule write handlers have no reachable UI path. Remove those local definitions. Keep scheduling in Facilities, which already owns its calendar and imports the required calendar stylesheet. Do not remove or change any backend schedule endpoint. Share the selected roster-role decision between filtering and row display. Keep failed new-role drafts visible and require an explicit retry instead of another automatic request.

The Finance display slice uses the codebase-design skill. Private render functions own the line-item display, payroll actions, accounting settings, and dialogs. They return elements directly. They do not define nested React component types that would remount controls. Existing handlers retain request ownership. The payroll table and detail dialog share accounting status decisions. Account ranking keeps the same types, keywords, and weights. No financial rule or provider operation changed.

The account mapping draft follows the saved connection ID and mapping fields. A guarded state update replaces the draft only when those values change. Status-only refreshes retain local edits. This removes the previous effect dependency warning without clearing the draft on every connection response. A workflow test edits the mapping, refreshes the connection status, and then loads a changed saved mapping. Keep all changes in issue 123 paths. The production preview remains unchanged.

The Finance slice keeps network requests and UI state in the existing panel. Payroll preparation, profitability grouping, and line-item draft validation move to focused data modules. These modules have no provider or browser dependency. Tests can exercise amount calculations, period filters, CSV escaping, and save payloads directly. This slice does not change accounting provider integration, database writes, or runtime state. The complete Finance panel still needs later controller and view cleanup before commit.

`organizationPayrollModel.ts` owns staff choices, whole-pay-run filtering, ledger totals, and update payload preparation. A staff filter selects matching pay runs, not individual items. The ledger and CSV still include every item from each selected pay run. Keep the existing inclusive date-overlap rules and optional update-field semantics. `organizationFinanceCsv.ts` owns CSV escaping and payroll columns. The journal preview export uses the same escaping helper. `organizationProfitabilityModel.ts` groups signed amounts by the existing event and team identity rules. `organizationFinanceLineItems.ts` owns cost drafts, category choices, validation, and save bodies. `organizationFinanceDates.ts` preserves local date conversion. `organizationFinanceTypes.ts` contains the existing local data types. No backend response or financial write contract changed.

Use cohesive helpers and internal modules to reduce branching. Keep data loading, permissions, persistence, and existing service contracts unchanged. Do not move code only to avoid the lint check: every new JavaScript or TypeScript file must pass the same rules.

Run Jest from one agent at a time. Run final TypeScript and lint after all edits are integrated. Ask before using parallel implementation agents. The review skill separately requires parallel Standards and Spec reviewers.

The controller cleanup uses pure modules for response mapping, permission decisions, staff roster presentation, and calendar range edits. It uses separate view modules for customer bills and documents. The page keeps the existing action handlers and data state. This keeps save behavior and input state unchanged while reducing the number of responsibilities in the page. The complete changed-file gate still applies to the old controllers and every new module.

The bill-card extraction exposed an existing click guard that matched the card itself. The corrected guard ignores only nested controls. A behavior test verifies mouse and keyboard editing and separate refund and cancellation actions. Customer mapping now reports missing bill-owner and registration-team names instead of displaying their record identifiers. No financial amount or HTTP contract changed.

The event cache now owns request generations, pagination, and hidden-event filtering. The page keeps filter controls and their input state. The existing shared Organization/Discover filter module still decides when a filter change needs a server refresh. A background refresh retains the loaded cache. New requests invalidate older responses. A synchronous request guard prevents duplicate pagination calls. Hidden-event updates change the cache only when a visible record is removed. The initial read waits for the matching Organization record so rental resources are available. The existing per-field range interface has no equivalent batch range read in the inspected service. This slice keeps that HTTP contract unchanged.

The Facilities staff-edit slice returns validated drafts, assignment updates, and occurrence-scope choices from pure functions. The UI applies those results through the existing undo queue. Resource context and occurrence matching now have separate modules. New-assignment submission keeps its existing service calls and payloads. Invalid calendar dates now produce an explicit form error before any request. A missing edited draft also produces an explicit error.

The Create Team slice uses `createTeamDraft.ts` for draft transitions, validation, and service arguments. `CreateTeamFields.tsx` owns the form sections. `useTeamDocumentOptions.ts` keeps document loading and retry scoped to the organization. It rejects incomplete template responses and ignores stale responses. A failed template read no longer appears as an empty document list. The form retains its draft during retries and blocks creation until the read succeeds. Registration price rules and service arguments remain unchanged. The shared image controls now support keyboard selection and block competing actions during upload. The existing image upload and user-update HTTP operations remain unchanged.

The checkout slice is limited to issue 123. Organization Store calls PaymentModal. Organization Facilities composes RentalReservationCheckout. `usePaymentDialogState.ts` owns the existing result callbacks and billing-profile state. `StripePaymentCheckout.tsx` keeps the existing Stripe initialization and Elements options. `PaymentResultView.tsx` and `RentalReservationDialogs.tsx` own presentation. PaymentForm now uses the shared payment button and surface tokens. No Stripe request, amount calculation, rental-order request, or lock operation changed. Tests check application state through the internal billing service and rental API boundaries. They do not simulate Stripe. The old PaymentModal tests that mocked the Stripe SDK were replaced with callback, refresh-failure, pending-result, reset, and cancelled-load tests.

The Discounts and Refunds slice is limited to issue 123 and its shared components. Private Discounts views separate pricing fields, selected-item details, and data-dependent results. `refundRequestReferences.ts` filters requests by the existing scope rules and loads their display names. `RefundRequestListView.tsx` preserves the shared requester and host table, payment scope, and actions. The controller still uses the existing refund service for decisions.

One intentional failure-state change applies to Refunds. Failed or incomplete name reads now show an error and withhold the affected requests. They no longer show record identifiers or usernames as required display names. The controller publishes requests and their names together. It clears previous requests when the scope changes and ignores late results. Filters remain mounted. This does not change an approval preview, amount, permission check, or decision payload.

Keep the existing event detail reads for refund references. The batch event list in `src/app/api/events/route.ts` excludes archived records and applies list visibility filters. The detail read in `src/app/api/events/[eventId]/route.ts` is not equivalent. A batch cutover could omit historical refund events. This slice deduplicates event detail reads and retains the existing user, organization, and team batch interfaces. It does not add or change backend endpoints.

The Divisions and Public Page slice uses the codebase-design skill to separate draft preparation from rendering. `organizationDivisionModel.ts` owns defaults, sport-dependent skill selection, price conversion, and display labels. `OrganizationDivisionViews.tsx` owns the heading, table, registration rows, and editor. The panel retains its existing list and write services. Required catalog labels now produce an explicit row error instead of a record ID. Missing optional selections display as not specified. Catalog loading keeps the heading and table headings visible. The editor captures field values before state updates. Failed saves retain the draft and show an accessible error inside the dialog. Pending saves block edits and dismissal.

`organizationPublicSettingsModel.ts` owns initial draft values, slug readiness, preview guidance, and the save payload. Public Page draft state follows the source Organization with a guarded state update. Edited values survive same-Organization refreshes. An Organization change resets those values. Widget event selections keep the same reset rules. `WidgetEventSearchPicker.tsx` retains the existing debounced search and stale-response guards. `organizationWidgetSnippets.ts` produces URL parameters and script attributes from one option mapping. These changes preserve the saved settings, filter semantics, snippet order, and HTTP requests. No provider interface, backend endpoint, financial workflow, runtime, or database changed.

Inspection found an existing persistence risk. `handleSaveManagerCalendarDrafts` sends rental and staff changes through separate requests. Direct multi-resource staff creation also uses separate requests. A later request failure can leave earlier writes saved. This slice does not add or change those HTTP operations. A complete fix needs one backend transaction for the related save. Record this risk during the remaining save-controller work. Do not describe the current calendar save as atomic.

## Outcomes & Retrospective

On 2026-09-07, the first production build passed TypeScript and generated all 127 static pages. The local production preview used the existing isolated database. Codex browser inspection confirmed the populated Customers list and separate rows for overlapping bookings. The console had no errors. Click, accessibility click, coordinate click, and Tab input did not change focus or activate controls. Live interaction verification remains open. A 390-pixel viewport exposed shrinkage of the All filter. A one-line flex rule corrects the source; the second build is in progress. The full Jest run was attempted once and then stopped after unrelated event-schedule failures and the known `MouseEvent` Node setup failures. Do not report the full suite as passed. Its partial log is `C:/Users/samue/AppData/Local/Temp/issue123-closeout-tests.log`. Separate reviewers are unavailable in this tool session. The independent review and issue closure remain open.

The complexity-policy adjustment passes its 12 real-ESLint regression tests. They verify all supported non-JSX extensions, the JSX/TSX threshold, non-blocking UI warnings, blocking nesting errors, and blocking conditional Hook calls. TypeScript and `git diff --check` pass. The changed-file command reports zero errors and eight warnings under the new policy. This is a policy change, not evidence that every behavior, provider, reference-approval, or release gate is complete. No runtime, database, build, or deployment changed.

The Facilities controller cleanup passed all 130 tests in nine suites. Its report is `apps/site/node_modules/.cache/issue-123-facilities-controller-final-tests.json`. The complete Facilities controller, save module, and tests pass strict lint. The full changed-file gate reports 30 errors and one image warning across 137 files. The remaining errors are in the Organization and Discover page controllers. The two new UI tests verify rejected rental drags and failed-save recovery, including pending-save protection, Undo, and explicit retry. The first rental-drag fixture placed the booking outside rental availability. It was corrected to put the booking inside availability and after the initial selection. No production behavior changed to satisfy that fixture. TypeScript and `git diff --check` pass. No runtime, build, database, environment, provider, or HTTP contract changed. The active preview does not include this source slice. Issue-wide acceptance checks remain open.

The staff range and rental checkout slice passed all 109 tests in seven suites. Its final report is `apps/site/node_modules/.cache/issue-123-fields-range-checkout-final-tests.json`. TypeScript and `git diff --check` pass. The new source modules and their tests pass strict focused lint. The final complete changed-file gate reports 35 errors and one image warning across 135 files. Five errors remain in the Facilities controller. The first checkout model run had two incorrect timestamp expectations that omitted seconds. The existing date formatter includes seconds. The corrected expectations pass without changing timestamp behavior. No runtime, database, environment, build, HTTP, or provider operation changed. The running preview does not include these source changes. The remaining issue-wide acceptance gates stay open.

The final Facilities interaction/editor run passed all 110 tests in six suites. Its report is `apps/site/node_modules/.cache/issue-123-fields-editor-tests.json`. This includes the real calendar card and grid tests, plus the complete Facilities workflow and pure schedule tests. TypeScript and `git diff --check` pass. The complete changed-file gate now reports 39 errors and one image warning across 131 files. Nine errors remain in Facilities. The display functions, editor setup, pointer activation, and move/resize dispatch now meet the limit. Hydration, range preparation, checkout, and save controllers remain. No runtime, database, environment, build, provider, or HTTP contract changed. The running preview does not include this source slice. Issue-wide acceptance checks remain open.

The Facilities occurrence and display slice passed all 84 tests in four suites. The final report is `apps/site/node_modules/.cache/issue-123-fields-render-tests.json`. It covers recurrence bounds, coverage gaps, calendar navigation, drag, resize, draft edits, input focus, reopen, and undo. TypeScript and `git diff --check` pass. The complete changed-file check reports 47 errors and one image warning across 131 files. Remaining errors are in the Organization, Facilities, and Discover controllers. No rule suppression or exception was added. The running preview was not rebuilt or restarted. Browser, provider, build, reference approval, and independent issue-wide reviews remain open.

The combined Facility details and Discover event slice passed all 68 tests in four suites. Its final report is `apps/site/node_modules/.cache/issue-123-details-discover-final-tests.json`. TypeScript passes. Strict focused lint uses the same complexity limit of 10 and nesting limit of four as `lint:changed`. The initial plain ESLint check did not enable the strict complexity override. The full gate found one remaining selection helper in Facility details. That helper is now split and passes the strict check. The final complete changed-file gate reports 56 errors and 15 warnings. No rule suppression or exception was added. Remaining failures are in the Organization page, Facilities controller, and Discover page. Earlier release-build, browser, provider, and independent review results do not validate this source slice.

The Staff and calendar-card slice reduced the changed-file gate from 72 errors and 17 warnings to 65 errors and 16 warnings. The first calendar run passed 70 tests in three suites. TypeScript then found an event union that needed an explicit selection check. After the fix, TypeScript passes and the focused Staff/calendar rerun passed all 22 tests in three suites. The failed-role regression first reproduced two creation requests after one asynchronous failure. The fixed draft stays visible until the user retries. Staff and the calendar-card files pass complete-file lint.

The Facility details slice passed all 50 tests in two suites. Its six new workflows prove equivalent-source draft and focus retention, source-change reset, failed-save retry, pending-save protection, whole-draft validation before writes, saved-resource identity mapping, and read-only navigation. TypeScript, focused lint, and `git diff --check` pass. The running preview remains unchanged. Browser, provider, build, independent review, and issue-wide gates remain open.

Work is in progress. The previous loading slice passed focused tests, TypeScript, the production build, and a 14-tab Codex browser check. Those results do not replace verification of this refactor.

The review fixed point is `1e1cb02b716ea74389170af29543b76d2ea0e902`. Standards and Spec reviews included committed and uncommitted issue work. Follow-up reviews checked the fixes. Both final follow-ups have no open findings in their named scope. These targeted results do not complete the issue-wide gates. The named shared dialog paths now use owned controls: BillingAddressModal, BillingAddressFields, CreateTeamModal, ImageUploader, ImageSelectionModal, PaymentModal, PaymentForm, and RentalReservationCheckout. Their tests cover loading, retry, save failure, address suggestions, state validation, registration prices, organization changes, image selection, payment-result callbacks, and rental completion actions. The issue-wide dependency audit and quality checks remain open. These source updates have not had release-build or Codex browser verification. Stripe sandbox verification is also pending. No runtime or database changed.

The final affected run passed 140 tests in 15 suites. It includes Facilities, Finance, Reviews, Public Page, both template tabs, Refunds, the shared shell and controls, Organization data loading, team labels, and Organization/Discover event filtering. TypeScript and `git diff --check` pass. Separate Discounts and CreateFieldModal checks passed three tests after the Discounts test was updated to open its create dialog. Extra coverage checks retained controls, failed refreshes, request ordering, remounted filter scope, rental classification, past-date query expansion, resource-only moves, short entries, and clipped drop regions.

The full Jest run reported 519 passing suites, 398 failing suites, 2679 passing tests, and 18 failing tests. Of the failing suites, 377 fail at setup because PointerEventMock extends MouseEvent in Node environments. That setup exists at the review base. Other failures include Playwright discovery, platform-specific CI and affiliate tests, event editing, weekly selection, and UI tests. The Discover and Discounts failures were addressed and their affected reruns pass. CreateFieldModal passes in isolation. Do not report the full suite as green. Its report is `apps/site/node_modules/.cache/issue-123-test-results.json`.

The earlier complete changed-file gate reported 128 errors and 20 warnings across 58 files. Large controllers still fail the strict complete-file limit. Later results appear below. The new shared loading, filter, presentation, and calendar helpers pass their focused check. No suppression or rule exception was added. Do not commit or close issue 123 while the gate fails.

The Codex in-app browser checked the existing production preview at the Document Templates tab. Refresh kept the heading, actions, filters, and table headings visible. It showed loading values and table-row placeholders. The existing template then returned. Two earlier chat-group timeout messages were present in the console. No Chrome window was opened. This turn did not rebuild or restart the preview, change the database, or deploy. The running build does not include this turn's source cleanup.

The later controller-extraction slice reduced the complete complexity check from 128 errors and 20 warnings to 107 errors and 18 warnings. The new modules pass their focused complexity checks. The remaining errors are in the larger controllers. The targeted review and full Jest results above predate this slice. Its final combined run passed 76 tests in 10 suites. TypeScript and `git diff --check` pass. The tests cover customer mapping, access decisions, bill and document actions, staff roster presentation, calendar ranges, Facilities, Customers, Staff, and the Organization page. No commit, runtime operation, database change, or deployment is part of this continuation.

The event-request and Reviews slice reduced the complete check from 107 errors to 102 errors. The warning count remains 18. Its final combined run passed 46 tests in six suites. These suites cover event queries, overlapping refreshes, duplicate pagination, hidden-event changes, Organization changes, shared event filtering, Reviews save/delete races, Organization data loading, and the shared shell. TypeScript and `git diff --check` pass. The new hidden-event test exposed a repeated state update when a caller supplied an equivalent array. The fixed update retains the current state when no event is removed. The complete Reviews panel and all new event files pass complexity. No commit or issue closure is permitted while the complete gate fails. This slice did not rebuild, restart, deploy, or change the database. The active production preview does not include these source changes. Earlier browser and review results do not validate this slice.

The Facilities staff-edit slice reduced the complete check from 102 errors to 97 errors across 80 changed files. The warning count remains 18. The former staff-submit function had complexity 115. Each of its focused workflows now meets the limit of 10. The complete Facilities file still has other failing functions. All 72 tests in four affected suites pass. The tests cover staff rates and repeat dates, invalid selections, draft scope, child identity, parent resource changes, occurrence matching, pending unassignment restoration, resource coordinates, undo, drag, resize, and calendar range edits. TypeScript and `git diff --check` pass. The existing Facilities suite passed before and after the final integration. No API, Prisma, mobile, database, runtime, build, or deployment change was made. Earlier browser and independent review results do not validate this slice.

The Divisions and Public Page slice reduced the complete check from 93 errors to 84 errors. The warning count remains 18. Its final combined run passed all 23 tests in six suites. Tests cover catalog loading, label failures, division creation and update payloads, failed-save retry, pending-save protection, archive actions, public registration actions, draft retention, slug readiness, stale widget search, and shared widget filter output. TypeScript, focused complete-file lint, and `git diff --check` pass. The final test report is `apps/site/node_modules/.cache/issue-123-divisions-public-settings-results.json`. An earlier rerun had one failure. Both the two-suite isolation run and the final combined run passed without further source changes. No browser or provider verification is claimed. The production preview remains unchanged.

The Finance display slice reduced the complete changed-file gate from 79 errors and 18 warnings to 72 errors and 17 warnings. The complete Finance panel and its workflow test file have no lint findings. Private render functions now separate dialogs, line items, ledgers, payroll history, and accounting settings. The table and detail dialog share pay-run accounting decisions. The account mapping draft retains local edits during status-only refreshes. Changed saved mappings replace the draft.

All 53 tests in five affected suites pass. The tests cover loading and focus retention, local validation, save failure and retry, payroll filtering and actions, exports, accounting settings, and mapping refresh behavior. The first run passed all 52 existing tests. Its new test failed because a query matched two status labels. The corrected query passes the complete workflow. TypeScript found an overly broad return type in the extracted profit-color helper. The helper now returns its required literal union. Final TypeScript, focused complete-file lint, and `git diff --check` pass. The test report is `apps/site/node_modules/.cache/issue-123-finance-views-tests.json`. No request handler changed except equivalent finance-query preparation and the mapping setter dependency. No provider, browser, or release-build verification is claimed. No runtime, database, commit, or deployment changed. The active preview does not include this source slice. The remaining issue-wide acceptance gates stay open.

## Context and Orientation

Current checkpoint: the user-approved complexity policy is active. The changed-file gate passes with zero errors, seven UI complexity warnings, and one image warning. The historical error counts below use the previous blanket limit of 10. They are not current blockers.

`apps/site/src/app/organizations/[id]/fieldsTab/facilityCalendarSave.ts` owns the existing queued calendar requests. `saveFacilityCalendarChanges` accepts the Organization ID, resource fields, source staff assignments, local drafts, rental updates, and staff overrides. It returns updated rental fields, created and updated staff assignments, and removed assignment IDs. It throws on the first failed awaited operation. It does not roll back earlier HTTP requests. `FieldsTabContent.tsx` publishes the returned records and clears the queue only after success. Private functions now separate hydration merging, manager range changes, and public rental range validation.

The Finance data-preparation slice reduced the complete changed-file gate from 84 errors to 79 errors. The warning count remains 18. Its final combined run passed all 52 tests in five suites. Tests cover local validation, failed-save draft retention and retry, payroll filtering, signed ledger totals, profitability grouping, local date conversion, CSV column order and escaping, optional update fields, and the existing Finance workflows. The export workflow test now waits for loaded pay-run data because its button stays visible during loading. TypeScript caught a shared journal CSV helper reference during extraction. The final shared CSV module resolves that reference. TypeScript, the focused new-module and test lint check, and `git diff --check` pass. The report is `apps/site/node_modules/.cache/issue-123-finance-model-tests.json`. The Finance panel still has seven complexity errors and one dependency warning. No production build, browser check, provider check, runtime change, or database change occurred. The active preview does not include this slice.

The Discounts and Refunds slice reduced the complete check from 97 errors to 93 errors. The warning count remains 18. All 19 tests in five suites pass. Tests cover synchronized discount prices, failed-save draft retention, local search during loading, code generation and deactivation, refund scope and decisions, missing display names, repeated references, and late responses after an Organization change. TypeScript and `git diff --check` pass. The complete changed files in this slice pass complexity. The production preview was not rebuilt or restarted. No browser or provider verification is claimed for this source slice. The remaining issue-wide checks stay open.

`apps/site/src/components/organization/organization-operation-ui.tsx` contains the owned controls. `apps/site/src/app/organizations/[id]/page.tsx` coordinates tabs, organization data, customers, billing actions, and dialogs. `FieldsTabContent.tsx`, `OrganizationFinancePanel.tsx`, `RoleRosterManager.tsx`, and the settings panels contain large operation controllers. Supporting calendar modules are in the `fieldsTab` directory. The existing plans `organization-tab-reference-alignment-execplan.md` and `organization-tab-data-loading-execplan.md` describe the reviewed UI behavior.

The Organization route directory now has `organizationCustomerModel.ts` for customer response mapping, `organizationAccess.ts` for permissions and available tabs, and `organizationStaffPresentation.ts` for roster and host labels. `OrganizationCustomerBills.tsx` and `OrganizationCustomerDocuments.tsx` own their display and local action controls. `organizationCustomerPresentation.ts` shares display formatting. `fieldsTab/facilityCalendarClassification.ts` owns layer and card classification. `fieldsTab/facilityCalendarRanges.ts` owns validated draft and assignment range edits. Tests cross these modules through their mapping functions, permission decisions, range edit functions, or user controls.

`organizationEventSource.ts` owns event query construction, hosted and rental reads, and visible-record merging. `useOrganizationEvents.ts` owns the event cache and request lifecycle. `OrganizationReviewsPanel.tsx` keeps its existing request and mutation handlers. Private view functions now own its summary, review action, notices, editor, and confirmation dialog.

`fieldsTab/facilityStaffEdits.ts` validates staff forms and prepares draft, assignment, and occurrence edits. `fieldsTab/facilityStaffOccurrences.ts` resolves occurrence ranges and finds restorable pending unassignments. `fieldsTab/facilityCalendarResources.ts` resolves facility links and coordinate precedence. `FieldsTabContent.tsx` keeps the UI state, applies prepared changes, and calls the existing save interfaces.

`fieldsTab/facilityStaffRangeChanges.ts` prepares calendar moves and resizes. It checks parent containment and returns child-before-parent overrides when an open shift shrinks. `fieldsTab/facilityRentalCheckout.ts` prepares rental bounds, selected resource details, callback data, and the matching schedule URL. The controller still owns the undo queue, notifications, and checkout callback. The shared resource and range modules own target-resource lookup and date parsing.

The Finance panel delegates payroll preparation, CSV formatting, profitability rows, and custom cost validation to the `organizationFinance*` and `organizationPayrollModel.ts` modules beside it. Its existing request handlers still own network calls, errors, refreshes, and dialog state. Private render functions own the display sections. Focused helpers own account ranking, mapping draft synchronization, transaction labels, and shared payroll accounting status. The complete panel now meets the complexity limit.

## Plan of Work

First, remove repeated style and presentation branches from the shared controls. Then extract coherent validation, mapping, scheduling, payroll, and customer operations from the large controllers. Keep the public props stable where possible. Extract view sections at stable internal interfaces so refreshing data does not replace controls or clear drafts.

Review each file group with its existing behavior tests. Remove only tests that are demonstrably redundant under the documented testing standard. Do not weaken tests or change financial behavior to satisfy lint. Finish with the full changed-file gate, affected suites, TypeScript, and separate code-review reports. Check the issue's reference approval and delivery conditions before any closure.

## Concrete Steps

Run the policy regression suite with `node node_modules/jest/bin/jest.js --runInBand --runTestsByPath scripts/__tests__/complexityPolicy.test.ts`. Expect 12 passing tests. The suite executes ESLint against in-memory source through its real command-line interface. Run `npm run lint:changed` to verify that warnings do not fail the shared gate.

The final Facilities check used these nine test paths: `src/app/organizations/[id]/__tests__/FieldsTabContent.test.tsx`, `src/app/organizations/[id]/fieldsTab/FacilityResourceCalendarGrid.test.tsx`, `src/app/organizations/[id]/fieldsTab/FacilityCalendarPanel.test.tsx`, `src/app/organizations/[id]/fieldsTab/facilityCalendarSave.test.ts`, `src/app/organizations/[id]/fieldsTab/facilityCalendarRanges.test.ts`, `src/app/organizations/[id]/fieldsTab/facilityCalendarOccurrences.test.ts`, `src/app/organizations/[id]/fieldsTab/facilityStaffRangeChanges.test.ts`, `src/app/organizations/[id]/fieldsTab/facilityRentalCheckout.test.ts`, and `src/app/organizations/[id]/fieldsTab/facilityCalendarResources.test.ts`. Pass those quoted paths to `node node_modules/jest/bin/jest.js --runInBand --runTestsByPath`. Expect 130 passing tests in nine suites. The saved report is `apps/site/node_modules/.cache/issue-123-facilities-controller-final-tests.json`.

Run site commands from `apps/site`. Use `npm run lint:changed`, `npx tsc --noEmit`, and `npx jest --runInBand --runTestsByPath` for affected test paths. Run `git diff --check` from the repository root. Use the canonical GitHub repository `Razumly/bracketiq` through the `gh` CLI. Use only the Codex in-app browser for UI checks.

## Validation and Acceptance

The changed-file check must report no errors. Existing behavior tests must still prove search and draft retention, data loading, permission decisions, schedule edits, persistence, and financial actions. The code review must not leave unresolved findings. Approval evidence must exist for every completed surface or the unresolved surface must remain open with an explicit decision. Do not close issue 123 while a required acceptance criterion is unresolved.

## Idempotence and Recovery

Do not discard existing edits. Do not merge or modify the coordinator's main worktree without the required coordination. Do not alter the database or running preview for a source cleanup. Stage only verified issue-owned files. Record failed checks and unresolved approvals in this plan.

## Artifacts and Notes

The final commit is pending. No HTTP, Prisma, or mobile contract changed. Three presence-only wrapper tests were removed under the testing standard. Their committed versions remain recoverable in Git; workflow tests remain.

Revision on 2026-09-05: record the Create Team and image-picker migration. Replace the old image-wrapper presence test with selection, removal, read-only, validation, and upload-failure tests. Keep the remaining checkout dialogs and issue-wide quality gates open.

Revision on 2026-09-05: record the payment and rental dialog migration for Organization Store and Facilities. Preserve financial behavior and HTTP contracts. Keep runtime, provider, visual, and issue-wide acceptance checks open.

Revision on 2026-09-05: record the Discounts and Refunds controller slice, its explicit reference-load failure state, 19 passing tests, and the remaining 93 complexity errors. Keep all changes scoped to issue 123. Keep the production preview unchanged.

Revision on 2026-09-05: record the verified Divisions and Public Page slice. Record the accessible division save error, pending-save guard, shared widget option mapping, and 23 passing tests. The complete source files pass complexity. The remaining workstream count is 84 errors and 18 warnings. Keep the issue-wide acceptance checks open. Keep the production preview unchanged.

Revision on 2026-09-05: record the verified Finance data-preparation slice. Preserve its financial calculations and requests. Record all 52 passing tests, TypeScript, focused lint, and the remaining 79 complexity errors. Keep the remaining Finance views, request controllers, and issue-wide acceptance checks open. Keep the running preview unchanged.

Revision on 2026-09-05: record the verified Finance display slice. Record 53 passing tests, TypeScript, and clean Finance lint. Keep the remaining 72 issue-wide errors and 17 warnings open. Preserve financial actions, the running preview, and the database. Final release-build, Codex browser, provider, reference approval, and issue-wide review checks remain pending.

The earlier verification and remaining acceptance work are recorded in [issue 123](https://github.com/Razumly/bracketiq/issues/123#issuecomment-5549083475). That comment predates the dialog migrations recorded here. Its shared-UI checklist item remains unchecked pending the remaining audit and verification. The issue remains open.

## Interfaces and Dependencies

Keep React and the existing BracketIQ-owned controls. Use internal application services as test seams. Do not introduce provider mocks, new runtime dependencies, rule suppressions, or alternate styling systems.

Plan revision: Record the Facilities staff-edit slice, its 72 passing tests, and the remaining 97 complexity errors. Record the existing partial-save risk. Keep the save controllers, shared-dialog migration, reference approvals, and issue-wide acceptance gates open. Keep the running preview unchanged.

Revision on 2026-09-05: Record the verified Staff, calendar-card, Facility details, and Discover event slices. Record explicit role-create retry, saved-resource identity mapping, retained filter inputs, and failed-refresh handling. The final combined details/event run passed 68 tests. The separate Staff/calendar rerun passed 22 tests. TypeScript and `git diff --check` pass. The complete changed-file gate still has 56 errors and 15 warnings. Keep the remaining controller work and issue-wide acceptance checks open. Keep the production preview unchanged.

Revision on 2026-09-05: Record the Facilities occurrence and display cleanup. Record 84 passing tests and the remaining 47 errors and one warning. Keep calendar interactions, save controllers, and issue-wide acceptance checks open. Preserve the running preview and all issue-owned changes.

Revision on 2026-09-05: Record the verified Facilities interaction and editor cleanup. Record 110 passing tests, TypeScript, and the remaining 39 errors and one warning. Keep the nine remaining Facilities controller errors and other issue-wide checks open. Do not treat this source verification as a production-build or browser result.

Revision on 2026-09-05: Record the staff range and rental checkout extraction. Record 109 passing tests, TypeScript, and strict focused lint. The full changed-file gate reports 35 errors and one warning. Preserve resource fallback rules, repeat dates, child coverage, queued changes, and checkout data. Keep the remaining Facilities hydration, selection, and save coordination work open. Preserve the running preview.

Revision on 2026-09-05: Recover the interrupted save validation. Record the completed Facilities controller cleanup, 130 passing tests, and the 30 remaining issue-wide errors. Record the rental-drag validation timing fix and failed-save recovery coverage. Keep the partial-save risk, release-build check, Codex browser check, provider checks, reference approval, and issue-wide reviews open. Preserve the running preview.

Revision on 2026-09-05: Apply the user-approved JSX/TSX complexity policy. Record the non-blocking threshold of 20, retained non-JSX limit of 10, retained nesting gate, and 12 real-ESLint tests. Replace score-driven UI extraction work with review of actual responsibilities and behavior. Keep the current Organization extraction verification and issue-wide acceptance checks open.
